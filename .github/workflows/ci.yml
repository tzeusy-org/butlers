name: CI

on:
  push:
    branches:
      - main
  pull_request:
  # The merge queue (bu-r5mnn) runs this workflow against the exact tree it
  # is about to land; that run is the terminal broad gate. The concurrency
  # group below already keys on github.ref, which is unique per queue entry.
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    # Path classification for pull_request gating (bu-r5mnn). Docs/spec-only
    # PRs skip the backend shards and the frontend jobs; everything else runs
    # them. Fail-closed rule: `backend` is true unless EVERY changed file
    # matches the `docs` filter, so an unknown path (a new top-level dir, a
    # dotfile, a binary, an empty file list from an API hiccup) always runs the
    # shards. `frontend` is true when anything under frontend/ changed.
    # merge_group and push events get `true` for both without consulting the
    # filter: the consumers decide per event what to do with that (merge_group
    # runs everything; push:main skips the shards by design because the merge
    # queue already validated this exact tree).
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      backend: ${{ steps.classify.outputs.backend }}
      frontend: ${{ steps.classify.outputs.frontend }}

    steps:
      - uses: actions/checkout@v6

      - name: Filter changed paths
        id: filter
        if: github.event_name == 'pull_request'
        uses: dorny/paths-filter@v3
        with:
          list-files: json
          filters: |
            docs:
              - 'about/**'
              - 'openspec/**'
              - 'docs/**'
              - '*.md'
              - '.beads/**'
              - 'LICENSE'
            all:
              - '**'

      - name: Classify the diff (fail closed)
        id: classify
        env:
          EVENT_NAME: ${{ github.event_name }}
          ALL_FILES: ${{ steps.filter.outputs.all_files }}
          DOCS_FILES: ${{ steps.filter.outputs.docs_files }}
        run: |
          python3 - <<'PY'
          import json, os

          event = os.environ["EVENT_NAME"]
          if event != "pull_request":
              backend = frontend = True
              reason = f"{event} event: no path filtering"
          else:
              all_files = json.loads(os.environ.get("ALL_FILES") or "[]")
              docs_files = set(json.loads(os.environ.get("DOCS_FILES") or "[]"))
              non_docs = sorted(path for path in all_files if path not in docs_files)
              if not all_files:
                  backend = frontend = True
                  reason = "empty changed-file list: failing closed"
              else:
                  backend = bool(non_docs)
                  frontend = any(path.startswith("frontend/") for path in all_files)
                  reason = (
                      f"{len(all_files)} changed file(s), {len(non_docs)} outside the docs filter"
                      + (f": {', '.join(non_docs[:10])}" if non_docs else "")
                  )
          print(f"backend={str(backend).lower()} frontend={str(frontend).lower()} ({reason})")
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as handle:
              handle.write(f"backend={str(backend).lower()}\n")
              handle.write(f"frontend={str(frontend).lower()}\n")
          PY

  plan:
    # Affected-test lane planner (bu-v28ho, follow-up to bu-r5mnn). Computes
    # whether this PR's diff can be safely scoped to only the tests
    # `butlers.testing.scoped_runner` maps to the changed files, via
    # `scripts/ci_test_plan.py`. Runs only for a pull_request whose diff
    # already reached beyond the docs filter (see `changes`); merge_group and
    # push:main are untouched by this job and keep running (or skipping, per
    # their own existing rules) the full shard matrix. Any planner
    # uncertainty -- an escalation trigger, an empty plan, or a plan reaching
    # into tests/e2e/ -- reports mode=full, which check-unit-*/
    # check-integration-* treat exactly like today: this job only ever
    # recommends narrowing FROM the existing full-matrix default, never
    # widens or replaces it on its own say-so. See
    # about/craft-and-care/testing-and-verification.md for the measured
    # planner precision that justified shipping this lane.
    runs-on: ubuntu-latest
    needs: [changes]
    if: github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true'
    outputs:
      mode: ${{ steps.plan.outputs.mode }}
      test_paths: ${{ steps.plan.outputs.test_paths }}

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Plan affected tests
        id: plan
        env:
          # The base branch's commit SHA at trigger time, not the symbolic
          # `origin/<branch>` ref: precise even if the base branch moves
          # again before this job finishes, and it needs no assumption about
          # which remote-tracking refs `fetch-depth: 0` populated.
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: uv run python scripts/ci_test_plan.py --base "$BASE_SHA"

  guards:
    # Every fast, dependency-free gate in one job (bu-r5mnn): one checkout, one
    # Python, one Node. The nine former jobs each paid their own runner spin-up
    # and checkout to run a script that takes seconds. Each guard step keeps
    # the comment block that explains why it exists; those comments are
    # load-bearing repo knowledge. Steps run under `!cancelled()` so one failing
    # guard never hides the findings of the others; the last step fails the job
    # if any guard step failed. Runs on every event. The active
    # `main-merge-queue` ruleset requires `guards` alongside `check` and
    # `frontend` on the exact `merge_group` tree; unlike path-filtered
    # `frontend`, this job never skips.
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Set up Node 24
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Install OpenSpec 1.9.0
        run: npm install --global @fission-ai/openspec@1.9.0

      # Session-link guard (bu-mr5t5): fails the PR when a tool-session
      # link/footer (see scripts/session_link_guard.py) leaks into the PR title,
      # body, review comments, or non-trailer commit text on the current head.
      # Review-comment coverage is best-effort (falls back to an empty set on
      # any API hiccup) so a GitHub API blip never blocks the mandatory
      # title/body/commit checks. PR-only: the inputs do not exist on other events.
      - name: Write PR title and body to files
        if: ${{ github.event_name == 'pull_request' && !cancelled() }}
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
        run: |
          printf '%s' "$PR_TITLE" > "${RUNNER_TEMP}/pr_title.txt"
          printf '%s' "$PR_BODY" > "${RUNNER_TEMP}/pr_body.txt"

      - name: Fetch review comments (best-effort)
        if: ${{ github.event_name == 'pull_request' && !cancelled() }}
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          # Each call is independently best-effort: a failed/empty response
          # just means that surface contributes nothing, it never fails the
          # step. Raw (non-jq-filtered) JSON is dumped per surface; the combine
          # script below does the field mapping so no shell/jq quoting games
          # are needed here.
          gh api "repos/${REPO}/pulls/${PR_NUMBER}/comments" --paginate \
            > "${RUNNER_TEMP}/rc_review_comments.json" || true
          gh api "repos/${REPO}/issues/${PR_NUMBER}/comments" --paginate \
            > "${RUNNER_TEMP}/rc_issue_comments.json" || true
          gh api "repos/${REPO}/pulls/${PR_NUMBER}/reviews" --paginate \
            > "${RUNNER_TEMP}/rc_reviews.json" || true
          python3 scripts/combine_review_comment_sources.py \
            --output "${RUNNER_TEMP}/review_comments.json" \
            --input review-comment "${RUNNER_TEMP}/rc_review_comments.json" \
            --input issue-comment "${RUNNER_TEMP}/rc_issue_comments.json" \
            --input review "${RUNNER_TEMP}/rc_reviews.json"

      - name: Scan for tool-session links
        id: session_links
        if: ${{ github.event_name == 'pull_request' && !cancelled() }}
        run: |
          python3 scripts/session_link_guard.py \
            --pr-title-file "${RUNNER_TEMP}/pr_title.txt" \
            --pr-body-file "${RUNNER_TEMP}/pr_body.txt" \
            --commit-range "${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}" \
            --review-comments-file "${RUNNER_TEMP}/review_comments.json"

      # Em-dash guard (bu-370uj): enforces non-negotiable #6 ("no em-dashes in
      # doctrine or dashboard copy") over the doctrine markdown corpus (about/
      # essays + roster/*/MANIFESTO.md + roster/*/AGENTS.md). A per-file
      # baseline (scripts/em-dash-baseline.json) freezes pre-existing debt so
      # the gate lands green today while blocking any net-new em-dash and any
      # new dirty file. Frontend user-facing copy is enforced by the eslint
      # rule in the `frontend` job instead.
      - name: Check for em-dashes in doctrine + dashboard copy
        id: em_dashes
        if: ${{ !cancelled() }}
        run: python3 scripts/check-no-em-dashes.py

      # Spec-overwrite guard (bu-s9uv3): `openspec archive` writes the WHOLE
      # requirement block from a change into openspec/specs/, so an unarchived
      # `## MODIFIED Requirements` block authored against an older ancestor
      # silently deletes whatever the baseline gained in between. OpenSpec 1.9.0
      # guards only scenario NAMES, so a block that keeps every name and rewrites
      # the bodies validates clean and still destroys baseline content -- which has
      # happened three times on the same axis. This compares bodies. A digest-keyed
      # ratchet (scripts/spec-overwrite-baseline.json) freezes today's debt and
      # re-fires as soon as an archive moves a requirement under an unarchived
      # block.
      - name: Check unarchived MODIFIED spec blocks against the live baseline
        id: spec_overwrites
        if: ${{ !cancelled() }}
        run: python3 scripts/check_spec_overwrites.py

      # Strict OpenSpec validation gate for bu-n58sv. The repository carries
      # both canonical specs and active deltas; validating both prevents
      # RFC-2119/prose and scenario-loss debt from accumulating behind scoped
      # per-change checks. The CLI version is pinned above so local and hosted
      # results use the same parser and strictness rules.
      - name: Validate all OpenSpec artifacts in strict mode
        id: openspec_strict
        if: ${{ !cancelled() }}
        run: make check-openspec-strict

      # Archived-requirements guard (bu-966by): the other side of the ledger from
      # the spec-overwrite guard. Archiving a change is supposed to write its
      # requirements into openspec/specs/, and 488 of them across 44 changes never
      # got there while every check stayed green -- `openspec validate` reads delta
      # syntax, `openspec archive` reports what it wrote and not what it skipped,
      # and check_spec_overwrites.py never looks at the baseline side at all. This
      # asserts each archived `## ADDED` requirement exists in the baseline by name
      # and each `## MODIFIED` block's content reached it, failing per requirement
      # so a half-applied archive cannot look green. A digest-keyed ratchet
      # (scripts/archived-requirements-baseline.json) freezes today's debt for
      # bu-tk618 to burn down by hand; there is no re-freeze flag on purpose,
      # and a `## REMOVED` block excuses an absence only once its own change
      # has archived, so a pending proposal cannot mute a gap off-ratchet.
      - name: Check archived change requirements reached the baseline
        id: archived_requirements
        if: ${{ !cancelled() }}
        run: python3 scripts/check_archived_requirements_landed.py

      # Countable-tasks guard (bu-h7igs): `openspec archive`'s incomplete-task
      # gate counts markdown checkbox lines only. A tasks.md written as `### N.`
      # sections with per-task acceptance bullets -- richer than most checkbox files
      # here, not lazier -- matches zero of them, so archive reports
      # `Task status: No tasks`, cannot be incomplete, and archives unprompted. The
      # defect is not the missing warning but that the gate's silence is
      # indistinguishable from its pass: "archived with no task warning" reads as
      # evidence the tasks were done. This fails on any unarchived change the gate
      # cannot see, so the difference is restored before the change reaches archive.
      - name: Check every change's tasks.md is countable by the archive gate
        id: countable_tasks
        if: ${{ !cancelled() }}
        run: python3 scripts/check_countable_tasks.py

      # Cited-requirements guard (bu-lpwjc): the third side of the ledger. The
      # two guards above read the spec tree against itself and have never opened a
      # test file, so nothing ever checked that a REQ id cited by a test docstring
      # resolves to a definition at all. Measured at introduction: 39 distinct ids
      # are cited, 30 resolve only to a change that has not archived yet, and
      # REQ-cli-runtime-auth-003 resolves nowhere -- there is no cli-runtime-auth
      # capability in the repo. openspec/specs/ and unarchived changes are the two
      # definition sources; openspec/changes/archive/** deliberately is NOT one,
      # because an archived change has already had its chance to write the
      # requirement into the baseline. Counting it would make this blind to
      # exactly the case it exists for (`openspec archive --skip-specs` moves the
      # delta and writes no baseline) and would hide a half-applied archive, since
      # the ids that landed and the ids that did not would read the same.
      # Pending-only citations pass but are printed with the change they lean on:
      # that is the state that rots. A ratchet
      # (scripts/cited-requirements-baseline.json) freezes today's one dangling
      # citation per (test file, id); there is no re-freeze flag on purpose.
      - name: Check REQ ids cited by tests resolve to a definition
        id: cited_requirements
        if: ${{ !cancelled() }}
        run: python3 scripts/check_cited_requirements_resolve.py

      # Frontend-copy-inventory guard (bu-erfdj): about/lay-and-land/frontend-copy-inventory.md
      # is generated by scripts/extract-frontend-copy.py, is committed so copy changes
      # surface in review, and tells readers not to edit it by hand -- but nothing ever
      # regenerated it, so it drifted silently while presenting itself as authoritative.
      # Committing the inventory only pays off if it is guaranteed current, so this
      # regenerates it from the checked-out frontend sources and fails on any diff.
      # The generator parses .tsx files directly (no node_modules, no npm install) and
      # its output is a pure function of those files with no timestamp or other
      # nondeterministic field, so this gate cannot flap.
      - name: Regenerate the frontend copy inventory
        id: frontend_copy_regenerate
        if: ${{ !cancelled() }}
        run: python3 scripts/extract-frontend-copy.py

      - name: Check the committed inventory matches the regenerated one
        id: frontend_copy
        if: ${{ !cancelled() }}
        run: |
          if ! git diff --exit-code -- about/lay-and-land/frontend-copy-inventory.md; then
            echo "::error file=about/lay-and-land/frontend-copy-inventory.md::Frontend copy inventory is stale. Regenerate it with 'python3 scripts/extract-frontend-copy.py' and commit the result."
            exit 1
          fi

      # Duplicate-name guard (bu-ayrbg): two branches each added a
      # module-level `_fenced_code_blocks` to one test file with incompatible
      # return types. The definitions were far apart, git auto-merged them, and
      # the later one silently shadowed the earlier one for every caller --
      # including a security guard that then iterated the characters of a string
      # and returned [] for every input, so a runnable pg_restore.sh in a fenced
      # block would have passed REQ-database-security-006 unnoticed.
      #
      # The non-obvious part is that `ruff check` is green on that tree. F811
      # (redefinition of unused name) does not fire when the FIRST definition is
      # USED between the two definitions -- the name is not "unused" at the point
      # of redefinition. That intervening-use condition is F811's blind spot, and
      # it is exactly what a merge produces, since each branch defines the name
      # and uses it. Per-PR CI cannot see it either: both PRs are green alone and
      # CI never builds the union of open PRs.
      #
      # This asserts each module-level name is bound once per file, independent of
      # use. It scans the lint gate's own scope (src/, tests/, roster/,
      # conftest.py) and fails if it finds zero Python files, so it cannot go
      # quiet the way the shadowed guard did. Ratchet:
      # scripts/duplicate-toplevel-names-baseline.json, shipped empty, with no
      # re-freeze flag on purpose.
      - name: Check for duplicate module-level names
        id: duplicate_names
        if: ${{ !cancelled() }}
        run: python3 scripts/check_duplicate_toplevel_names.py

      - name: Fail if any guard failed
        if: ${{ !cancelled() }}
        env:
          GUARD_OUTCOMES: |
            session_links=${{ steps.session_links.outcome }}
            em_dashes=${{ steps.em_dashes.outcome }}
            spec_overwrites=${{ steps.spec_overwrites.outcome }}
            openspec_strict=${{ steps.openspec_strict.outcome }}
            archived_requirements=${{ steps.archived_requirements.outcome }}
            countable_tasks=${{ steps.countable_tasks.outcome }}
            cited_requirements=${{ steps.cited_requirements.outcome }}
            frontend_copy_regenerate=${{ steps.frontend_copy_regenerate.outcome }}
            frontend_copy=${{ steps.frontend_copy.outcome }}
            duplicate_names=${{ steps.duplicate_names.outcome }}
        run: |
          failed=0
          while IFS= read -r line; do
            [ -n "$line" ] || continue
            case "$line" in
              *=success) ;;
              *=skipped) echo "skipped: $line" ;;
              *)
                echo "::error::guard failed: $line"
                failed=1
                ;;
            esac
          done <<< "$GUARD_OUTCOMES"
          exit "$failed"

  # Preserve the historic `check` status as a fail-closed fan-in. The
  # one-time preflight and ten duration-balanced file shards all begin
  # independently; no broad lane serializes the backend critical path.
  check-preflight:
    runs-on: ubuntu-latest
    needs: [changes]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`). push:main: skipped by design,
    # the merge queue already validated this exact tree.
    if: github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true')

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Check lock file is up to date
        run: uv lock --check

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Lint
        run: make lint

      - name: Format check
        run: uv run ruff format --check src/ tests/ roster/ conftest.py -q

      - name: SQL safety check (FOR UPDATE + outer join)
        run: make check-for-update-joins

      - name: Verify CI test shard manifests
        # This replaces the old integration-only path guard. It derives both
        # marker selections with pytest --collect-only and rejects gaps,
        # duplicates, stale files, and zero-selected manifests.
        run: uv run python scripts/check_ci_test_shards.py

      - name: Check test budget per lane
        # Ratchet (bu-r5mnn): each lane's collected-test count must stay under
        # scripts/test-budget-baseline.json. Growth past the headroom is fixed
        # in the same PR by condensing, or raised deliberately with
        # --update-baseline plus a stated net test delta in the PR body.
        run: uv run python scripts/check_test_budget.py

      - name: Smoke tests (fast gate + release evidence)
        id: smoke
        env:
          SMOKE_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/smoke
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: |
          mkdir -p "$SMOKE_EVIDENCE_DIR"
          SMOKE_CMD="uv run pytest tests/ --ignore=tests/e2e -m smoke -q --tb=short"

          START_TS=$(date +%s)
          set +e
          $SMOKE_CMD --junit-xml="$SMOKE_EVIDENCE_DIR/raw-junit.xml" 2>&1 | tee "$SMOKE_EVIDENCE_DIR/smoke.log"
          SMOKE_EXIT=${PIPESTATUS[0]}
          set -e
          END_TS=$(date +%s)
          DURATION_S=$((END_TS - START_TS))
          [ "$SMOKE_EXIT" -eq 0 ] && STATUS=PASS || STATUS=FAIL

          # Write release-evidence JSON and emit a clearly-formatted log line.
          SMOKE_EXIT_VAL=$SMOKE_EXIT \
          SMOKE_SHA="${{ github.sha }}" \
          SMOKE_DURATION=$DURATION_S \
          SMOKE_STATUS=$STATUS \
          SMOKE_CMD_VAL="$SMOKE_CMD" \
          SMOKE_EVIDENCE_DIR="$SMOKE_EVIDENCE_DIR" \
          python3 << 'PYEOF'
          import json, os, sys, xml.etree.ElementTree as ET

          sha = os.environ["SMOKE_SHA"]
          duration_s = int(os.environ["SMOKE_DURATION"])
          exit_code = int(os.environ["SMOKE_EXIT_VAL"])
          status = os.environ["SMOKE_STATUS"]
          cmd = os.environ["SMOKE_CMD_VAL"]
          evidence_dir = os.environ["SMOKE_EVIDENCE_DIR"]

          try:
              root = ET.parse(os.path.join(evidence_dir, "raw-junit.xml")).getroot()
              skipped_classes = sorted({
                  tc.get("classname") or tc.get("name", "unknown")
                  for tc in root.findall(".//testcase")
                  if tc.find("skipped") is not None
              })
          except (ET.ParseError, OSError) as exc:
              print(exc, file=sys.stderr)
              skipped_classes = ["unavailable"]

          evidence = {
              "cmd": cmd,
              "sha": sha,
              "duration_s": duration_s,
              "status": status,
              "exit_code": exit_code,
              "skipped_classes": skipped_classes,
          }
          with open(os.path.join(evidence_dir, "release-evidence.json"), "w") as f:
              json.dump(evidence, f, indent=2)

          print(f"SMOKE-RELEASE-EVIDENCE cmd={cmd!r} sha={sha!r} duration={duration_s}s status={status!r} skipped_classes={skipped_classes!r}")
          print()
          print(json.dumps(evidence, indent=2))
          PYEOF

          exit $SMOKE_EXIT

      - name: Prepare smoke test duration evidence
        if: ${{ always() && steps.smoke.outcome != 'skipped' }}
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/smoke/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/smoke/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/smoke/top-durations.txt" \
            --limit 25

      - name: Upload smoke release evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-release-evidence
          path: ${{ runner.temp }}/ci-artifacts/smoke/release-evidence.json
          retention-days: 30
          if-no-files-found: warn
          overwrite: true

      - name: Upload smoke test evidence
        if: ${{ always() && steps.smoke.outcome != 'skipped' }}
        uses: actions/upload-artifact@v4
        with:
          name: ci-smoke-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/smoke/junit.xml
            ${{ runner.temp }}/ci-artifacts/smoke/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

  check-unit-1:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Unit tests (shard 1)
        # File-only manifests preserve whole-file fixture lifetime. The selector
        # owns the current marker expression and explicitly keeps xdist loadfile.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-unit-1.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/unit-1
        run: uv run python scripts/check_ci_test_shards.py run --lane unit --shard 1

      - name: Prepare unit shard 1 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/unit-1/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/unit-1/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/unit-1/top-durations.txt" \
            --limit 25

      - name: Upload unit shard 1 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-1-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/unit-1/junit.xml
            ${{ runner.temp }}/ci-artifacts/unit-1/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload unit shard 1 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-1-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-unit-1.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-unit-2:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Unit tests (shard 2)
        # File-only manifests preserve whole-file fixture lifetime. The selector
        # owns the current marker expression and explicitly keeps xdist loadfile.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-unit-2.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/unit-2
        run: uv run python scripts/check_ci_test_shards.py run --lane unit --shard 2

      - name: Prepare unit shard 2 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/unit-2/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/unit-2/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/unit-2/top-durations.txt" \
            --limit 25

      - name: Upload unit shard 2 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-2-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/unit-2/junit.xml
            ${{ runner.temp }}/ci-artifacts/unit-2/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload unit shard 2 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-2-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-unit-2.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-unit-3:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Unit tests (shard 3)
        # File-only manifests preserve whole-file fixture lifetime. The selector
        # owns the current marker expression and explicitly keeps xdist loadfile.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-unit-3.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/unit-3
        run: uv run python scripts/check_ci_test_shards.py run --lane unit --shard 3

      - name: Prepare unit shard 3 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/unit-3/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/unit-3/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/unit-3/top-durations.txt" \
            --limit 25

      - name: Upload unit shard 3 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-3-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/unit-3/junit.xml
            ${{ runner.temp }}/ci-artifacts/unit-3/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload unit shard 3 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-3-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-unit-3.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-unit-4:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Unit tests (shard 4)
        # File-only manifests preserve whole-file fixture lifetime. The selector
        # owns the current marker expression and explicitly keeps xdist loadfile.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-unit-4.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/unit-4
        run: uv run python scripts/check_ci_test_shards.py run --lane unit --shard 4

      - name: Prepare unit shard 4 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/unit-4/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/unit-4/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/unit-4/top-durations.txt" \
            --limit 25

      - name: Upload unit shard 4 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-4-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/unit-4/junit.xml
            ${{ runner.temp }}/ci-artifacts/unit-4/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload unit shard 4 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-4-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-unit-4.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-unit-5:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Unit tests (shard 5)
        # File-only manifests preserve whole-file fixture lifetime. The selector
        # owns the current marker expression and explicitly keeps xdist loadfile.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-unit-5.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/unit-5
        run: uv run python scripts/check_ci_test_shards.py run --lane unit --shard 5

      - name: Prepare unit shard 5 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/unit-5/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/unit-5/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/unit-5/top-durations.txt" \
            --limit 25

      - name: Upload unit shard 5 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-5-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/unit-5/junit.xml
            ${{ runner.temp }}/ci-artifacts/unit-5/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload unit shard 5 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-unit-5-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-unit-5.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-integration-1:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Free disk space before testcontainers
        # Runner disk capacity varies. Preserve the cleanup that eliminated
        # no-space/asyncpg failures on small runners, but do not spend minutes
        # deleting 20+ GiB of tools when the runner already has a safe floor.
        run: |
          echo "Disk usage before cleanup:"
          df -h /
          MIN_FREE_GB=30
          available_kib=$(df -Pk / | awk 'NR == 2 {print $4}')
          case "$available_kib" in
            ''|*[!0-9]*)
              echo "::error::could not determine free disk space"
              exit 1
              ;;
          esac
          available_gb=$((available_kib / 1024 / 1024))
          if [ "$available_gb" -lt "$MIN_FREE_GB" ]; then
            echo "Cleaning disk: ${available_gb} GiB is below ${MIN_FREE_GB} GiB."
            sudo rm -rf /usr/local/lib/android
            sudo rm -rf /usr/share/dotnet
            sudo rm -rf /opt/ghc
            sudo rm -rf /usr/local/.ghcup
            sudo rm -rf /usr/share/swift
            sudo rm -rf /usr/local/share/boost
            sudo apt-get clean -q
            docker image prune -f --filter "until=1h" || true
          else
            echo "Skipping disk cleanup: ${available_gb} GiB meets ${MIN_FREE_GB} GiB floor."
          fi
          echo "Disk usage after cleanup decision:"
          df -h /

      - name: Integration tests (shard 1)
        # The preflight job proves all integration-selected files belong to
        # exactly one manifest before these Docker/testcontainers shards run.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-integration-1.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/integration-1
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: uv run python scripts/check_ci_test_shards.py run --lane integration --shard 1

      - name: Prepare integration shard 1 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/integration-1/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/integration-1/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/integration-1/top-durations.txt" \
            --limit 25

      - name: Upload integration shard 1 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-1-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/integration-1/junit.xml
            ${{ runner.temp }}/ci-artifacts/integration-1/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload integration shard 1 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-1-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-integration-1.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-integration-2:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Free disk space before testcontainers
        # Runner disk capacity varies. Preserve the cleanup that eliminated
        # no-space/asyncpg failures on small runners, but do not spend minutes
        # deleting 20+ GiB of tools when the runner already has a safe floor.
        run: |
          echo "Disk usage before cleanup:"
          df -h /
          MIN_FREE_GB=30
          available_kib=$(df -Pk / | awk 'NR == 2 {print $4}')
          case "$available_kib" in
            ''|*[!0-9]*)
              echo "::error::could not determine free disk space"
              exit 1
              ;;
          esac
          available_gb=$((available_kib / 1024 / 1024))
          if [ "$available_gb" -lt "$MIN_FREE_GB" ]; then
            echo "Cleaning disk: ${available_gb} GiB is below ${MIN_FREE_GB} GiB."
            sudo rm -rf /usr/local/lib/android
            sudo rm -rf /usr/share/dotnet
            sudo rm -rf /opt/ghc
            sudo rm -rf /usr/local/.ghcup
            sudo rm -rf /usr/share/swift
            sudo rm -rf /usr/local/share/boost
            sudo apt-get clean -q
            docker image prune -f --filter "until=1h" || true
          else
            echo "Skipping disk cleanup: ${available_gb} GiB meets ${MIN_FREE_GB} GiB floor."
          fi
          echo "Disk usage after cleanup decision:"
          df -h /

      - name: Integration tests (shard 2)
        # The preflight job proves all integration-selected files belong to
        # exactly one manifest before these Docker/testcontainers shards run.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-integration-2.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/integration-2
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: uv run python scripts/check_ci_test_shards.py run --lane integration --shard 2

      - name: Prepare integration shard 2 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/integration-2/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/integration-2/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/integration-2/top-durations.txt" \
            --limit 25

      - name: Upload integration shard 2 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-2-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/integration-2/junit.xml
            ${{ runner.temp }}/ci-artifacts/integration-2/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload integration shard 2 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-2-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-integration-2.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-integration-3:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Free disk space before testcontainers
        # Runner disk capacity varies. Preserve the cleanup that eliminated
        # no-space/asyncpg failures on small runners, but do not spend minutes
        # deleting 20+ GiB of tools when the runner already has a safe floor.
        run: |
          echo "Disk usage before cleanup:"
          df -h /
          MIN_FREE_GB=30
          available_kib=$(df -Pk / | awk 'NR == 2 {print $4}')
          case "$available_kib" in
            ''|*[!0-9]*)
              echo "::error::could not determine free disk space"
              exit 1
              ;;
          esac
          available_gb=$((available_kib / 1024 / 1024))
          if [ "$available_gb" -lt "$MIN_FREE_GB" ]; then
            echo "Cleaning disk: ${available_gb} GiB is below ${MIN_FREE_GB} GiB."
            sudo rm -rf /usr/local/lib/android
            sudo rm -rf /usr/share/dotnet
            sudo rm -rf /opt/ghc
            sudo rm -rf /usr/local/.ghcup
            sudo rm -rf /usr/share/swift
            sudo rm -rf /usr/local/share/boost
            sudo apt-get clean -q
            docker image prune -f --filter "until=1h" || true
          else
            echo "Skipping disk cleanup: ${available_gb} GiB meets ${MIN_FREE_GB} GiB floor."
          fi
          echo "Disk usage after cleanup decision:"
          df -h /

      - name: Integration tests (shard 3)
        # The preflight job proves all integration-selected files belong to
        # exactly one manifest before these Docker/testcontainers shards run.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-integration-3.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/integration-3
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: uv run python scripts/check_ci_test_shards.py run --lane integration --shard 3

      - name: Prepare integration shard 3 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/integration-3/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/integration-3/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/integration-3/top-durations.txt" \
            --limit 25

      - name: Upload integration shard 3 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-3-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/integration-3/junit.xml
            ${{ runner.temp }}/ci-artifacts/integration-3/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload integration shard 3 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-3-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-integration-3.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-integration-4:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Free disk space before testcontainers
        # Every Docker-heavy shard needs a small-runner guard, but runners that
        # already meet the floor skip the costly reclamation work.
        run: |
          echo "Disk usage before cleanup:"
          df -h /
          MIN_FREE_GB=30
          available_kib=$(df -Pk / | awk 'NR == 2 {print $4}')
          case "$available_kib" in
            ''|*[!0-9]*)
              echo "::error::could not determine free disk space"
              exit 1
              ;;
          esac
          available_gb=$((available_kib / 1024 / 1024))
          if [ "$available_gb" -lt "$MIN_FREE_GB" ]; then
            echo "Cleaning disk: ${available_gb} GiB is below ${MIN_FREE_GB} GiB."
            sudo rm -rf /usr/local/lib/android
            sudo rm -rf /usr/share/dotnet
            sudo rm -rf /opt/ghc
            sudo rm -rf /usr/local/.ghcup
            sudo rm -rf /usr/share/swift
            sudo rm -rf /usr/local/share/boost
            sudo apt-get clean -q
            docker image prune -f --filter "until=1h" || true
          else
            echo "Skipping disk cleanup: ${available_gb} GiB meets ${MIN_FREE_GB} GiB floor."
          fi
          echo "Disk usage after cleanup decision:"
          df -h /

      - name: Integration tests (shard 4)
        # The preflight job proves all integration-selected files belong to
        # exactly one manifest before these Docker/testcontainers shards run.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-integration-4.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/integration-4
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: uv run python scripts/check_ci_test_shards.py run --lane integration --shard 4

      - name: Prepare integration shard 4 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/integration-4/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/integration-4/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/integration-4/top-durations.txt" \
            --limit 25

      - name: Upload integration shard 4 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-4-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/integration-4/junit.xml
            ${{ runner.temp }}/ci-artifacts/integration-4/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload integration shard 4 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-4-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-integration-4.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-integration-5:
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # merge_group: always. pull_request: only when the diff reaches beyond the
    # docs filter (fail-closed, see `changes`) and the affected-test planner
    # (bu-v28ho) did not select a scoped plan -- a scoped plan means
    # check-affected is this shard's evidence instead. push:main: skipped by
    # design, the merge queue already validated this exact tree.
    # `plan` is conditionally skipped (it only runs on a backend-touching
    # pull_request), and GitHub Actions auto-skips a job whose `needs` were
    # skipped/failed UNLESS the job's `if:` uses always() -- a plain boolean
    # `if:` here would never even be evaluated on merge_group, silently
    # skipping every shard there. `always()` opts out of that implicit gate,
    # so `needs.changes.result == 'success'` re-implements the "changes must
    # have succeeded" half of it manually. Deliberately no equivalent check
    # on `plan`: a plan that failed (as opposed to being cleanly skipped)
    # must still fall through to the full matrix, which `mode != 'scoped'`
    # already guarantees since a failed/skipped plan's output is empty.
    if: always() && needs.changes.result == 'success' && (github.event_name == 'merge_group' || (github.event_name == 'pull_request' && needs.changes.outputs.backend == 'true' && needs.plan.outputs.mode != 'scoped'))

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Free disk space before testcontainers
        # Every Docker-heavy shard needs a small-runner guard, but runners that
        # already meet the floor skip the costly reclamation work.
        run: |
          echo "Disk usage before cleanup:"
          df -h /
          MIN_FREE_GB=30
          available_kib=$(df -Pk / | awk 'NR == 2 {print $4}')
          case "$available_kib" in
            ''|*[!0-9]*)
              echo "::error::could not determine free disk space"
              exit 1
              ;;
          esac
          available_gb=$((available_kib / 1024 / 1024))
          if [ "$available_gb" -lt "$MIN_FREE_GB" ]; then
            echo "Cleaning disk: ${available_gb} GiB is below ${MIN_FREE_GB} GiB."
            sudo rm -rf /usr/local/lib/android
            sudo rm -rf /usr/share/dotnet
            sudo rm -rf /opt/ghc
            sudo rm -rf /usr/local/.ghcup
            sudo rm -rf /usr/share/swift
            sudo rm -rf /usr/local/share/boost
            sudo apt-get clean -q
            docker image prune -f --filter "until=1h" || true
          else
            echo "Skipping disk cleanup: ${available_gb} GiB meets ${MIN_FREE_GB} GiB floor."
          fi
          echo "Disk usage after cleanup decision:"
          df -h /

      - name: Integration tests (shard 5)
        # The preflight job proves all integration-selected files belong to
        # exactly one manifest before these Docker/testcontainers shards run.
        env:
          COVERAGE_FILE: ${{ runner.temp }}/ci-coverage/coverage-integration-5.data
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/integration-5
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: uv run python scripts/check_ci_test_shards.py run --lane integration --shard 5

      - name: Prepare integration shard 5 duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/integration-5/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/integration-5/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/integration-5/top-durations.txt" \
            --limit 25

      - name: Upload integration shard 5 test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-5-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/integration-5/junit.xml
            ${{ runner.temp }}/ci-artifacts/integration-5/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

      - name: Upload integration shard 5 coverage data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-integration-5-coverage-data
          path: ${{ runner.temp }}/ci-coverage/coverage-integration-5.data
          retention-days: 7
          if-no-files-found: error
          overwrite: true

  check-affected:
    # Affected-test lane (bu-v28ho): the `plan` job's evidence when it
    # selected a scoped plan. Runs the planner-selected test paths directly
    # instead of the full ten-shard matrix. Not part of the coverage-combine
    # pipeline in `check` -- this lane trades combined coverage for wall
    # time on the PRs where the planner's selection is precise, and the
    # merge queue's merge_group run still exercises every shard with
    # coverage before anything lands on main.
    runs-on: ubuntu-latest
    needs: [changes, plan]
    # `plan` is conditionally skipped, so this job's `if:` must use always()
    # for the same reason the ten shards do (see their comment): a plain
    # boolean `if:` here would never even be evaluated when `plan` was
    # skipped or failed, and would happen to still be correct today only
    # because `plan` cannot succeed without `changes` having succeeded first
    # -- a chain of implication too fragile to rely on silently.
    if: always() && needs.changes.result == 'success' && github.event_name == 'pull_request' && needs.plan.outputs.mode == 'scoped'

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    env:
      DATABASE_URL: postgresql://postgres:test@localhost:5432/postgres

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Affected tests (scoped plan)
        env:
          TEST_PATHS_JSON: ${{ needs.plan.outputs.test_paths }}
          TEST_EVIDENCE_DIR: ${{ runner.temp }}/ci-artifacts/affected
          # The scoped selection may include integration/db-marked tests
          # (testcontainers), same as the check-integration-* shards.
          TESTCONTAINERS_RYUK_DISABLED: "true"
        run: |
          mkdir -p "$TEST_EVIDENCE_DIR"
          mapfile -t TEST_PATHS < <(python3 -c "import json, os; print('\n'.join(json.loads(os.environ['TEST_PATHS_JSON'])))")
          echo "Running affected-test lane over: ${TEST_PATHS[*]}"
          uv run pytest "${TEST_PATHS[@]}" -q --tb=short -n auto --dist loadfile \
            --junitxml="$TEST_EVIDENCE_DIR/raw-junit.xml"

      - name: Prepare affected-test duration evidence
        if: always()
        run: |
          python3 scripts/summarize_junit_durations.py \
            --input "${RUNNER_TEMP}/ci-artifacts/affected/raw-junit.xml" \
            --sanitized-junit "${RUNNER_TEMP}/ci-artifacts/affected/junit.xml" \
            --durations-output "${RUNNER_TEMP}/ci-artifacts/affected/top-durations.txt" \
            --limit 25

      - name: Upload affected-test evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-affected-test-evidence
          path: |
            ${{ runner.temp }}/ci-artifacts/affected/junit.xml
            ${{ runner.temp }}/ci-artifacts/affected/top-durations.txt
          retention-days: 14
          if-no-files-found: error
          overwrite: true

  check:
    # Do not allow a green historic check status when a preflight or any shard
    # failed, was skipped for an unexpected reason, or was cancelled. `always()`
    # is deliberate: a skipped historical check can read as green to branch
    # protection after a cancelled prerequisite. Three skips are accepted, and
    # only these three: (a) a pull_request whose `changes` job succeeded and
    # classified the diff as docs-only (`backend == 'false'`); (b) a push to
    # main, where the merge queue's merge_group run already validated this
    # exact tree; and (c) a pull_request where the affected-test planner
    # (bu-v28ho) selected a scoped plan, in which case `check-affected` is
    # required to be the shards' evidence instead. Everything else that is
    # not `success` fails the fan-in. Coverage aggregation and the badge only
    # run when the ten-shard matrix actually ran.
    needs: [changes, plan, check-preflight, check-unit-1, check-unit-2, check-unit-3, check-unit-4, check-unit-5, check-integration-1, check-integration-2, check-integration-3, check-integration-4, check-integration-5, check-affected]
    if: ${{ always() }}
    runs-on: ubuntu-latest

    steps:
      - name: Require preflight and every test shard to pass (or be skipped by the path filter)
        id: gate
        env:
          EVENT_NAME: ${{ github.event_name }}
          CHANGES_RESULT: ${{ needs.changes.result }}
          CHANGES_BACKEND: ${{ needs.changes.outputs.backend }}
          PLAN_MODE: ${{ needs.plan.outputs.mode }}
          CHECK_UNIT_1_RESULT: ${{ needs.check-unit-1.result }}
          CHECK_UNIT_2_RESULT: ${{ needs.check-unit-2.result }}
          CHECK_UNIT_3_RESULT: ${{ needs.check-unit-3.result }}
          CHECK_UNIT_4_RESULT: ${{ needs.check-unit-4.result }}
          CHECK_UNIT_5_RESULT: ${{ needs.check-unit-5.result }}
          CHECK_INTEGRATION_1_RESULT: ${{ needs.check-integration-1.result }}
          CHECK_INTEGRATION_2_RESULT: ${{ needs.check-integration-2.result }}
          CHECK_INTEGRATION_3_RESULT: ${{ needs.check-integration-3.result }}
          CHECK_INTEGRATION_4_RESULT: ${{ needs.check-integration-4.result }}
          CHECK_INTEGRATION_5_RESULT: ${{ needs.check-integration-5.result }}
          CHECK_AFFECTED_RESULT: ${{ needs.check-affected.result }}
        run: |
          skip_allowed=0
          skip_reason=""
          require_check_affected=0
          if [ "$EVENT_NAME" = "pull_request" ] && [ "$CHANGES_RESULT" = "success" ] && [ "$CHANGES_BACKEND" = "false" ]; then
            skip_allowed=1
            skip_reason="docs-only pull request: backend shards skipped by the path filter"
          elif [ "$EVENT_NAME" = "push" ]; then
            skip_allowed=1
            skip_reason="push to main: backend shards skipped by design, the merge queue validated this tree"
          elif [ "$EVENT_NAME" = "pull_request" ] && [ "$CHANGES_BACKEND" = "true" ] && [ "$PLAN_MODE" = "scoped" ]; then
            skip_allowed=1
            require_check_affected=1
            skip_reason="affected-test lane selected a scoped plan: heavy shards skipped, check-affected is the evidence"
          fi

          failed=0
          ran=0
          skipped=0
          # check-preflight is deliberately excluded from this loop: it always
          # runs and always succeeds when reached, so it is not one of the ten
          # heavy shards gated by the scoped/full plan decision. Counting its
          # success here as `ran=1` made every scoped-mode PR (the normal path
          # for most backend PRs) fail with a false "heavy shards ran but the
          # plan selected a scoped mode" error, even when every real shard
          # correctly reported skipped. check-preflight's own success/failure
          # is already a hard `needs:` dependency of this job, so a failed
          # preflight fails `check` through normal GitHub Actions dependency
          # semantics without needing to appear in this shard-consistency loop.
          for named_result in \
            "check-unit-1=$CHECK_UNIT_1_RESULT" \
            "check-unit-2=$CHECK_UNIT_2_RESULT" \
            "check-unit-3=$CHECK_UNIT_3_RESULT" \
            "check-unit-4=$CHECK_UNIT_4_RESULT" \
            "check-unit-5=$CHECK_UNIT_5_RESULT" \
            "check-integration-1=$CHECK_INTEGRATION_1_RESULT" \
            "check-integration-2=$CHECK_INTEGRATION_2_RESULT" \
            "check-integration-3=$CHECK_INTEGRATION_3_RESULT" \
            "check-integration-4=$CHECK_INTEGRATION_4_RESULT" \
            "check-integration-5=$CHECK_INTEGRATION_5_RESULT"; do
            case "$named_result" in
              *=success) ran=1 ;;
              *=skipped)
                if [ "$skip_allowed" -eq 1 ]; then
                  skipped=1
                else
                  echo "::error::$named_result (skipped, but this event requires the shards to run)"
                  failed=1
                fi
                ;;
              *)
                echo "::error::$named_result"
                failed=1
                ;;
            esac
          done

          if [ "$ran" -eq 1 ] && [ "$skipped" -eq 1 ]; then
            echo "::error::inconsistent shard state: some shards ran and some were skipped"
            failed=1
          fi
          if [ "$ran" -eq 1 ] && [ "$require_check_affected" -eq 1 ]; then
            echo "::error::heavy shards ran but the plan selected a scoped mode; inconsistent state"
            failed=1
          fi

          # check-affected has the inverse requirement: required exactly when
          # the plan selected scoped mode, skipped every other time (docs-only,
          # push:main, merge_group, or a full/escalated plan).
          case "$CHECK_AFFECTED_RESULT" in
            success)
              if [ "$require_check_affected" -ne 1 ]; then
                echo "::error::check-affected=success but this event/plan did not select the scoped lane"
                failed=1
              fi
              ;;
            skipped)
              if [ "$require_check_affected" -eq 1 ]; then
                echo "::error::check-affected=skipped but the plan selected a scoped mode"
                failed=1
              fi
              ;;
            *)
              echo "::error::check-affected=$CHECK_AFFECTED_RESULT"
              failed=1
              ;;
          esac

          if [ "$skipped" -eq 1 ] && [ "$failed" -eq 0 ]; then
            echo "::notice::$skip_reason"
          fi

          if [ "$ran" -eq 1 ] && [ "$failed" -eq 0 ]; then
            echo "shards_ran=true" >> "$GITHUB_OUTPUT"
          else
            echo "shards_ran=false" >> "$GITHUB_OUTPUT"
          fi
          exit "$failed"

      - uses: actions/checkout@v6
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}

      - name: Set up Python 3.12
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: astral-sh/setup-uv@v8.1.0

      - name: Install dependencies for coverage aggregation
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        run: uv sync --frozen --dev

      - name: Download unit shard 1 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-unit-1-coverage-data
          path: ${{ runner.temp }}/ci-coverage/unit-1

      - name: Download unit shard 2 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-unit-2-coverage-data
          path: ${{ runner.temp }}/ci-coverage/unit-2

      - name: Download unit shard 3 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-unit-3-coverage-data
          path: ${{ runner.temp }}/ci-coverage/unit-3

      - name: Download unit shard 4 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-unit-4-coverage-data
          path: ${{ runner.temp }}/ci-coverage/unit-4

      - name: Download unit shard 5 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-unit-5-coverage-data
          path: ${{ runner.temp }}/ci-coverage/unit-5

      - name: Download integration shard 1 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-integration-1-coverage-data
          path: ${{ runner.temp }}/ci-coverage/integration-1

      - name: Download integration shard 2 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-integration-2-coverage-data
          path: ${{ runner.temp }}/ci-coverage/integration-2

      - name: Download integration shard 3 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-integration-3-coverage-data
          path: ${{ runner.temp }}/ci-coverage/integration-3

      - name: Download integration shard 4 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-integration-4-coverage-data
          path: ${{ runner.temp }}/ci-coverage/integration-4

      - name: Download integration shard 5 coverage data
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/download-artifact@v4
        with:
          name: ci-integration-5-coverage-data
          path: ${{ runner.temp }}/ci-coverage/integration-5

      - name: Combine coverage from all independent test shards
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        id: coverage
        env:
          UNIT_1_COVERAGE: ${{ runner.temp }}/ci-coverage/unit-1/coverage-unit-1.data
          UNIT_2_COVERAGE: ${{ runner.temp }}/ci-coverage/unit-2/coverage-unit-2.data
          UNIT_3_COVERAGE: ${{ runner.temp }}/ci-coverage/unit-3/coverage-unit-3.data
          UNIT_4_COVERAGE: ${{ runner.temp }}/ci-coverage/unit-4/coverage-unit-4.data
          UNIT_5_COVERAGE: ${{ runner.temp }}/ci-coverage/unit-5/coverage-unit-5.data
          INTEGRATION_1_COVERAGE: ${{ runner.temp }}/ci-coverage/integration-1/coverage-integration-1.data
          INTEGRATION_2_COVERAGE: ${{ runner.temp }}/ci-coverage/integration-2/coverage-integration-2.data
          INTEGRATION_3_COVERAGE: ${{ runner.temp }}/ci-coverage/integration-3/coverage-integration-3.data
          INTEGRATION_4_COVERAGE: ${{ runner.temp }}/ci-coverage/integration-4/coverage-integration-4.data
          INTEGRATION_5_COVERAGE: ${{ runner.temp }}/ci-coverage/integration-5/coverage-integration-5.data
          COMBINED_COVERAGE: ${{ runner.temp }}/ci-coverage/coverage-combined.data
          COMBINED_REPORT: ${{ runner.temp }}/ci-artifacts/coverage/coverage.json
        run: |
          mkdir -p "$(dirname "$COMBINED_COVERAGE")" "$(dirname "$COMBINED_REPORT")"
          for coverage_file in \
            $UNIT_1_COVERAGE \
            $UNIT_2_COVERAGE \
            $UNIT_3_COVERAGE \
            $UNIT_4_COVERAGE \
            $UNIT_5_COVERAGE \
            $INTEGRATION_1_COVERAGE \
            $INTEGRATION_2_COVERAGE \
            $INTEGRATION_3_COVERAGE \
            $INTEGRATION_4_COVERAGE \
            $INTEGRATION_5_COVERAGE; do
            test -s "$coverage_file"
          done
          uv run coverage combine --data-file="$COMBINED_COVERAGE" \
            $UNIT_1_COVERAGE \
            $UNIT_2_COVERAGE \
            $UNIT_3_COVERAGE \
            $UNIT_4_COVERAGE \
            $UNIT_5_COVERAGE \
            $INTEGRATION_1_COVERAGE \
            $INTEGRATION_2_COVERAGE \
            $INTEGRATION_3_COVERAGE \
            $INTEGRATION_4_COVERAGE \
            $INTEGRATION_5_COVERAGE
          uv run coverage json --data-file="$COMBINED_COVERAGE" -o "$COMBINED_REPORT"
          uv run coverage report --data-file="$COMBINED_COVERAGE" --show-missing
          export COMBINED_REPORT
          COVERAGE=$(python3 -c 'import json, os; print(round(json.load(open(os.environ["COMBINED_REPORT"]))["totals"]["percent_covered"]))')
          echo "percentage=$COVERAGE" >> "$GITHUB_OUTPUT"
          # Color thresholds: green >=80, yellow >=60, red <60.
          if [ "$COVERAGE" -ge 80 ]; then
            echo "color=brightgreen" >> "$GITHUB_OUTPUT"
          elif [ "$COVERAGE" -ge 60 ]; then
            echo "color=yellow" >> "$GITHUB_OUTPUT"
          else
            echo "color=red" >> "$GITHUB_OUTPUT"
          fi

      - name: Upload combined coverage report
        if: ${{ steps.gate.outputs.shards_ran == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: ci-combined-coverage-report
          path: ${{ runner.temp }}/ci-artifacts/coverage/coverage.json
          retention-days: 14
          if-no-files-found: warn
          overwrite: true

      - name: Update coverage badge
        # Shards no longer run on push:main, so the merge queue's merge_group run
        # is the lane that still has coverage data for the tree about to land.
        # merge_group runs execute in the repository context and see repository
        # secrets; dynamic-badges-action needs only the gist secrets, not the ref.
        if: ${{ steps.gate.outputs.shards_ran == 'true' && github.event_name == 'merge_group' }}
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          auth: ${{ secrets.GIST_TOKEN }}
          gistID: ${{ secrets.COVERAGE_GIST_ID }}
          filename: butlers-coverage.json
          label: coverage
          message: ${{ steps.coverage.outputs.percentage }}%
          color: ${{ steps.coverage.outputs.color }}

  frontend:
    runs-on: ubuntu-latest
    needs: [changes]
    # merge_group and push:main: always (cheap, no coverage dependency).
    # pull_request: only when frontend/ changed (fail-closed, see `changes`).
    if: github.event_name == 'merge_group' || github.event_name == 'push' || (github.event_name == 'pull_request' && needs.changes.outputs.frontend == 'true')

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node 24
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Em-dash copy gate (bu-370uj)
        run: npm run lint:emdash

      - name: Query-result coercion gate (bu-ep4ks.5)
        run: npm run lint:query-coercion

      - name: Import graph (knip)
        run: npm run knip

      - name: Build
        run: npm run build

      - name: Test
        run: npm run test

  frontend-e2e:
    runs-on: ubuntu-latest
    needs: [changes]
    # merge_group and push:main: always (cheap, no coverage dependency).
    # pull_request: only when frontend/ changed (fail-closed, see `changes`).
    if: github.event_name == 'merge_group' || github.event_name == 'push' || (github.event_name == 'pull_request' && needs.changes.outputs.frontend == 'true')

    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node 24
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npm run test:e2e:install

      - name: Build (required for vite preview)
        run: npm run build

      - name: Run Playwright e2e tests
        run: npm run test:e2e

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 7
