# Docker Compose configuration for Butlers AI agent framework
#
# USAGE:
#
#   ./scripts/compose.sh                            # recommended dev launcher
#   ./scripts/compose.sh --hotreload               # with live code reload
#   ./scripts/compose.sh --prod                    # production database
#
# NOTE: compose.sh pre-builds the shared butlers-app image. A bare direct
# Compose invocation with this non-privileged base file deliberately excludes
# the restore-drill executor and its secret/network contract. If running the
# ordinary stack through docker compose directly, build the image first:
#   docker build -t butlers-app:latest .
#
# PROFILES:
#   (default)  — all core services: migrations, butlers-up, connectors,
#                dashboard-api, oauth-gate, log infrastructure
#                (postgres is external — configured via .env.dev / .env.prod)
#   dev        — + Vite frontend dev server
#   hotreload  — + volume-mount src/ for live code changes (scales down butlers-up
#                and dashboard-api, replaces with *-hotreload variants)
#   audio      — + live-listener connector (requires /dev/snd)
#   minio      — + local S3-compatible storage (production uses Garage on tailnet)
#
# PROD DEPLOYS:
#   `butlers deploy` (src/butlers/core/deploy.py) is the one-command idempotent
#   path: build the image with GIT_SHA, force-rerun `migrations` via `run --rm`
#   (never trust a stale exited container's `service_completed_successfully`
#   — see bu-zhfd0), recreate services, poll /health, record to
#   public.deployments. It NEVER passes --profile and always strips an
#   inherited COMPOSE_PROFILES from its own subprocess env, so the merged
#   config for a deploy is always the profile-less baked-image base set plus
#   the protected restore-drill fragment — the hotreload/dev profiles below
#   cannot leak into a prod deploy, regardless of ambient shell state. Do not
#   tag prod-relevant services with `profiles: [prod]` without updating both
#   scripts/compose.sh and src/butlers/core/deploy.py in lockstep.
#
# NETWORK MODEL (see RFC 0008):
#   db        (internal) — minio, migrations (external postgres via tailnet)
#
# DATABASE PROVISIONING:
#   Before first use, run scripts/init-db.sql against your PostgreSQL server
#   as a privileged cluster superuser. The normal POSTGRES_USER /
#   butlers.connecting_user must not be the active bootstrap identity; it
#   installs extensions, creates/grants managed schemas + roles, and configures
#   default privileges for the migration user:
#
#     psql -h <POSTGRES_HOST> -U <superuser> -d butlers \
#         -f scripts/init-db.sql
#
#   This is the one privileged bootstrap step; afterwards docker-compose
#   migrations can continue running as POSTGRES_USER (typically "butlers").
#   See scripts/README.md for details.
#   backend   (internal) — inter-service communication
#   frontend  (internal) — Vite dev server <-> dashboard-api
#   egress    (bridge)   — outbound internet for LLM APIs, Google OAuth, etc.
#   Private subnets blocked by scripts/egress-firewall.sh (auto-applied by launcher)

# ── Shared environment anchors ──────────────────────────────────────────

x-postgres-env: &postgres-env
  POSTGRES_HOST: ${POSTGRES_HOST:?Set POSTGRES_HOST in .env.dev or .env.prod}
  POSTGRES_PORT: ${POSTGRES_PORT:-5432}
  POSTGRES_USER: ${POSTGRES_USER:-butlers}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env.dev or .env.prod}
  POSTGRES_SSLMODE: ${POSTGRES_SSLMODE:-}
  BUTLERS_DB_POOL_MIN_SIZE: ${BUTLERS_DB_POOL_MIN_SIZE:-0}
  BUTLERS_DB_POOL_MAX_SIZE: ${BUTLERS_DB_POOL_MAX_SIZE:-4}

x-connector-env: &connector-env
  <<: *postgres-env
  SWITCHBOARD_MCP_URL: http://butlers-up:41100/sse
  OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}

# Note: runtime volume mounts (runtime_claude, etc.) are repeated per service
# because YAML anchors cannot merge into lists. This is a YAML limitation.

# ── Services ────────────────────────────────────────────────────────────

services:

  # ── Layer 0: Infrastructure ───────────────────────────────────────────

  # MinIO — local S3 (optional). Production uses Garage on tailnet.
  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    profiles: [minio]
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:-minioadmin}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:-minioadmin}"
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks: [db]

  minio-setup:
    image: minio/mc:RELEASE.2025-08-13T08-35-41Z
    profiles: [minio]
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:-minioadmin}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:-minioadmin}"
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 $$MINIO_ROOT_USER $$MINIO_ROOT_PASSWORD;
      mc mb local/butlers-blobs --ignore-existing;
      exit 0;
      "
    networks: [db]

  # ── Layer 0.5: Migrations + Log Init ──────────────────────────────────

  migrations:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    command: ["db", "migrate"]
    environment:
      <<: *postgres-env
    volumes:
      - ./roster:/app/roster:ro
    networks: [db]

  log-init:
    image: alpine:3.19
    entrypoint: ["sh", "-c"]
    command:
      - |
        STAMP="$$(date +%Y%m%d_%H%M%S)"
        mkdir -p "/logs/$$STAMP"
        rm -f /logs/latest
        ln -s "$$STAMP" /logs/latest
        echo "/app/logs/$$STAMP" > /logs/.current_run_dir
        echo "Log directory: /app/logs/$$STAMP"
    volumes:
      - ./logs:/logs

  # ── Log retention sidecar ─────────────────────────────────────────────
  # Per-run stdout dirs (logs/YYYYMMDD_HHMMSS/) are written by the app and
  # connector containers AS ROOT (they need HOME=/root for the LLM spawner
  # and uv cache), so the host user cannot prune them without sudo. This
  # sidecar runs as root with the same ./logs bind mount and reaps files
  # older than LOG_RETAIN_DAYS on a daily cron, then prunes the emptied
  # run dirs. It does an initial sweep on boot so a fresh stack reclaims
  # disk immediately instead of waiting for the first cron tick.
  #
  # Note: this only manages the unmanaged per-run output.log dirs. The
  # structured app logs under logs/{butlers,connectors,uvicorn} already
  # self-rotate weekly + gzip via src/butlers/core/logging.py; a 3-day
  # default would delete their .gz backups too, so retention is a coarse
  # disk guard, not the system of record for those.
  #
  # Default schedule: 04:00 daily.  Override via LOG_CLEANUP_CRON.
  # Default retention: 3 days.  Override via LOG_RETAIN_DAYS.
  log-cleanup:
    image: alpine:3.19
    user: root
    entrypoint: ["sh", "-c"]
    command:
      - |
        RETAIN="$${LOG_RETAIN_DAYS:-3}"
        CRON="$${LOG_CLEANUP_CRON:-0 4 * * *}"
        sweep() {
          find /logs -type f -mtime +"$$RETAIN" -delete
          find /logs -mindepth 1 -type d -empty -delete
        }
        echo "log-cleanup: retain=$${RETAIN}d schedule='$${CRON}' (initial sweep now)"
        sweep
        echo "$${CRON} find /logs -type f -mtime +$${RETAIN} -delete; find /logs -mindepth 1 -type d -empty -delete" \
          | crontab -
        exec crond -f -d 8
    volumes:
      - ./logs:/logs
    restart: unless-stopped

  # ── Layer 1a: Dashboard + Frontend ────────────────────────────────────

  # ── Backup cron sidecar ───────────────────────────────────────────────
  # Runs deploy/backup/pg_dump.sh on a daily cron schedule, writing
  # timestamped .sql.gz dumps to the butlers_backups volume.  The
  # dashboard-api reads that same volume to populate the BackupTile.
  #
  # Default schedule: 02:00 UTC daily.  Override via BACKUP_CRON env var.
  # Default retention: 14 days.  Override via BACKUP_RETAIN_DAYS env var.
  backup-cron:
    image: postgres:17-alpine
    user: root
    entrypoint: ["sh", "-c"]
    command:
      - |
        # Install crond (already present in alpine) and write crontab.
        # Run as root so crond can write its spool and redirect logs to
        # /proc/1/fd/1 (which requires root on Alpine).
        CRON="${BACKUP_CRON:-0 2 * * *}"
        echo "$${CRON} sh /backup/pg_dump.sh >> /proc/1/fd/1 2>&1" \
          | crontab -
        echo "backup-cron: schedule='$${CRON}'"
        exec crond -f -d 8
    environment:
      <<: *postgres-env
      POSTGRES_DB: ${POSTGRES_DB:-butlers}
      BACKUP_DIR: /backups
      BACKUP_RETAIN_DAYS: ${BACKUP_RETAIN_DAYS:-14}
      BACKUP_CRON: ${BACKUP_CRON:-0 2 * * *}
    volumes:
      - ./deploy/backup/pg_dump.sh:/backup/pg_dump.sh:ro
      - butlers_backups:/backups
    depends_on:
      migrations:
        condition: service_completed_successfully
    networks: [db]
    restart: unless-stopped

  dashboard-api:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    ports:
      - "127.0.0.1:${DASHBOARD_HOST_PORT:-41200}:41200"
    volumes:
      - ./roster:/app/roster:ro
      - ./logs:/app/logs
      - wa_bridge_socket:/tmp/wa-bridge
      - butlers_backups:/backups:ro
      # bu-hmdqz.6: same read-only single-file mount as butlers-up/
      # butlers-up-hotreload below -- GET /api/decisions
      # (src/butlers/api/routers/decisions.py) runs in THIS container and
      # calls compute_decision_digest() in-process, so without this mount
      # it is permanently decisions_available=False. Never mount the whole
      # .beads/ dir (see butlers-up's own volumes block for why).
      - ./.beads/issues.export.jsonl:/app/.beads/issues.export.jsonl:ro
    command: ["dashboard", "--host", "0.0.0.0", "--port", "41200"]
    environment:
      <<: *postgres-env
      GOOGLE_OAUTH_REDIRECT_URI: ${GOOGLE_OAUTH_REDIRECT_URI:-http://localhost:41200/api/oauth/google/callback}
      OAUTH_DASHBOARD_URL: ${OAUTH_DASHBOARD_URL:-}
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:-}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET:-}
      SPOTIFY_OAUTH_REDIRECT_URI: ${SPOTIFY_OAUTH_REDIRECT_URI:-}
      OWNTRACKS_CONNECTOR_HOST: ${OWNTRACKS_CONNECTOR_HOST:-}
      OWNTRACKS_CONNECTOR_PORT: ${OWNTRACKS_CONNECTOR_PORT:-}
      OWNTRACKS_WEBHOOK_BASE_PATH: ${OWNTRACKS_WEBHOOK_BASE_PATH:-/owntracks}
      BUTLERS_DISABLE_FILE_LOGGING: "1"
      BUTLERS_HOST: butlers-up
      WHATSAPP_BRIDGE_SOCKET: /tmp/wa-bridge/bridge.sock
      BUTLERS_BACKUP_DIR: /backups
      BUTLERS_API_DB_POOL_MIN_SIZE: ${BUTLERS_API_DB_POOL_MIN_SIZE:-0}
      BUTLERS_API_DB_POOL_MAX_SIZE: ${BUTLERS_API_DB_POOL_MAX_SIZE:-2}
      DASHBOARD_API_KEY: ${DASHBOARD_API_KEY:-}
      DASHBOARD_AUTH_DB_USER: ${DASHBOARD_AUTH_DB_USER:-}
      DASHBOARD_AUTH_DB_PASSWORD: ${DASHBOARD_AUTH_DB_PASSWORD:-}
      DASHBOARD_AUTH_ORIGIN: ${DASHBOARD_AUTH_ORIGIN:-}
      DASHBOARD_AUTH_RP_ID: ${DASHBOARD_AUTH_RP_ID:-}
      DASHBOARD_AUTH_DEPLOYMENT: ${DASHBOARD_AUTH_DEPLOYMENT:-default}
      DASHBOARD_AUTH_TRUSTED_PROXY_PEERS: ${DASHBOARD_AUTH_TRUSTED_PROXY_PEERS:-}
      # bu-g4jg1: butlers.jobs.external_deadman runs in THIS container and reads
      # this from os.environ. Unset/empty leaves the loop disabled (by design).
      # Without this passthrough, setting it in .env has no effect at all --
      # there is no env_file here, so only vars listed explicitly reach the
      # container. Point it at an off-host push/heartbeat monitor URL.
      EXTERNAL_DEADMAN_URL: ${EXTERNAL_DEADMAN_URL:-}
    # bu-0uqgo.11 (REQ-core-credentials-002): Dashboard is the only signing
    # side, so it is the only service that receives the private signer.  The
    # keyring below is public and shared with every verifying process; the
    # signer is not shared with anything.  Both are read once at process
    # start, so rotation is a restart, never a live file swap.
    secrets:
      - source: runtime_probe_control_signing_key
        target: runtime_probe_control_signing_key
        mode: 0400
    configs:
      - source: runtime_probe_control_verifiers
        target: /run/secrets/runtime_probe_control_verifiers
    depends_on:
      migrations:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:41200/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s
    # Dashboard-owned runtime CLI authentication uses a nested Bubblewrap
    # domain.  Keep this exact namespace-capable policy confined to the two
    # Dashboard services; ordinary Butler and restore-drill services retain
    # their existing security posture.
    security_opt:
      - apparmor:unconfined
      - systempaths=unconfined
      - seccomp:./deploy/seccomp/dashboard-runtime-cli-sandbox.json
    networks: [db, backend, frontend, egress]
    restart: unless-stopped

  frontend-dev:
    # Node pinned to 24 to match CI (.github/workflows/ci.yml node-version: "24").
    # ``./frontend`` is a host bind-mount, so the startup install writes through to
    # the tracked frontend/package-lock.json. ``npm ci`` (not ``npm install``)
    # installs strictly FROM the lockfile and never rewrites it -- it fails loud on
    # a lock/package.json mismatch instead of silently re-resolving. On Node 22
    # ``npm ci`` rejected this Node-24-generated lockfile (exit 1); Node 24 + npm ci
    # matches CI's own ``npm ci`` step exactly, so the lockfile stays the single
    # source of truth and startup can no longer dirty the worktree (bu-0zvsd).
    image: node:24-slim
    profiles: [dev]
    working_dir: /app
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    ports:
      - "127.0.0.1:${FRONTEND_HOST_PORT:-41173}:41173"
    environment:
      - VITE_API_URL=${VITE_API_URL:-/butlers-api/api}
      - VITE_PROXY_TARGET=http://dashboard-api:41200
      # CARTO's browser-visible basemap key is injected from BWS as
      # CARTO_BASEMAP_API_KEY and mapped into Vite's public env namespace.
      - VITE_CARTO_BASEMAP_API_KEY=${CARTO_BASEMAP_API_KEY:-}
    command: >-
      sh -c "npm ci &&
      npm run dev -- --host 0.0.0.0 --port 41173 --strictPort
      --base ${FRONTEND_BASE_PATH:-/butlers/}"
    depends_on:
      dashboard-api:
        condition: service_healthy
    networks: [frontend]
    restart: unless-stopped

  # ── Layer 1b: Connectors ──────────────────────────────────────────────

  connector-telegram-bot:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/telegram_bot", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.telegram_bot"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: telegram
      CONNECTOR_CHANNEL: telegram_bot
      # Apply gap-interview (bu-whhll.12) inline-button taps deterministically
      # via the chronicler dashboard API (the connector's own role cannot write
      # the chronicler schema). Override to disable the round-trip.
      CONNECTOR_INTERNAL_API_URL: ${CONNECTOR_INTERNAL_API_URL:-http://dashboard-api:41200}
      # Approval callback credentials are Tier-1 values read from the shared
      # credential store; do not pass them through Compose environment values.
    volumes:
      - ./logs:/app/logs
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    restart: unless-stopped

  connector-telegram-user:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/telegram_user", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.telegram_user_client"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: telegram
      CONNECTOR_CHANNEL: telegram_user_client
    volumes:
      - ./logs:/app/logs
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    restart: unless-stopped

  connector-whatsapp-user:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/whatsapp_user", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.whatsapp_user_client"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: whatsapp
      CONNECTOR_CHANNEL: whatsapp_user_client
      CONNECTOR_HEALTH_PORT: "40082"
      WA_BRIDGE_SOCKET: /tmp/wa-bridge/bridge.sock
    volumes:
      - ./logs:/app/logs
      - wa_bridge_socket:/tmp/wa-bridge
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    restart: unless-stopped

  connector-google-calendar:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/google_calendar", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.google_calendar"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: google_calendar
      CONNECTOR_CHANNEL: google_calendar
      CONNECTOR_HEALTH_PORT: "40085"
      CONNECTOR_BUTLER_DB_NAME: ${POSTGRES_DB:-butlers}
      BUTLER_SHARED_DB_NAME: ${POSTGRES_DB:-butlers}
      BUTLER_SHARED_DB_SCHEMA: shared
    volumes:
      - ./logs:/app/logs
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      oauth-gate:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    restart: unless-stopped

  connector-spotify:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/spotify", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.spotify"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: spotify
      CONNECTOR_CHANNEL: spotify
      CONNECTOR_HEALTH_PORT: "40083"
    volumes:
      - ./logs:/app/logs
    networks: [db, backend]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
      oauth-gate:
        condition: service_completed_successfully
    restart: unless-stopped

  connector-steam:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/steam", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.steam"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: steam
      CONNECTOR_CHANNEL: gaming
      CONNECTOR_HEALTH_PORT: "40089"
    volumes:
      - ./logs:/app/logs
    networks: [db, backend]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
      oauth-gate:
        condition: service_completed_successfully
    restart: unless-stopped

  connector-live-listener:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        EXTRAS: live-listener
        GIT_SHA: ${GIT_SHA:-unknown}
    image: butlers-app-audio:${BUTLERS_APP_TAG:-latest}
    profiles: [audio]
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/live_listener", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.live_listener"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: live-listener
      CONNECTOR_CHANNEL: voice
      CONNECTOR_HEALTH_PORT: "40091"
      LIVE_LISTENER_DEVICES: ${LIVE_LISTENER_DEVICES:-[{"name":"webcam","device":"hw:2,0"}]}
    volumes:
      - ./logs:/app/logs
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    devices:
      - /dev/snd:/dev/snd
    restart: unless-stopped

  connector-owntracks:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/owntracks", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.owntracks"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: owntracks
      CONNECTOR_CHANNEL: owntracks
      CONNECTOR_HEALTH_PORT: "40086"
    ports:
      - "127.0.0.1:${OWNTRACKS_HOST_PORT:-40086}:40086"
    volumes:
      - ./logs:/app/logs
    networks: [db, backend]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    restart: unless-stopped

  # ActivityWatch runs on the owner's desktop/laptop, not inside this stack.
  # ACTIVITYWATCH_BASE_URL must point at that machine's AW REST API (e.g. a
  # Tailscale MagicDNS hostname); ACTIVITYWATCH_MACHINE_ID identifies which
  # machine this connector instance polls. Multi-machine setups (e.g. a
  # separate work laptop) need one additional service block per machine —
  # not templated here (see bu-whhll.6 follow-ups).
  connector-activitywatch:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/activitywatch", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.activitywatch"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: activitywatch
      CONNECTOR_CHANNEL: activitywatch
      CONNECTOR_HEALTH_PORT: "40092"
      ACTIVITYWATCH_BASE_URL: ${ACTIVITYWATCH_BASE_URL:-http://localhost:5600}
      ACTIVITYWATCH_MACHINE_ID: ${ACTIVITYWATCH_MACHINE_ID:-desktop}
    ports:
      - "127.0.0.1:${ACTIVITYWATCH_HOST_PORT:-40092}:40092"
    volumes:
      - ./logs:/app/logs
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    restart: unless-stopped

  connector-home-assistant:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/home_assistant", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.home_assistant"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: home_assistant
      CONNECTOR_CHANNEL: home_assistant
      CONNECTOR_HEALTH_PORT: "40087"
      CONNECTOR_BUTLER_DB_NAME: ${POSTGRES_DB:-butlers}
      CONNECTOR_BUTLER_DB_SCHEMA: shared
      BUTLER_SHARED_DB_NAME: ${POSTGRES_DB:-butlers}
      BUTLER_SHARED_DB_SCHEMA: shared
      # Optional overrides (credentials resolved from CredentialStore by default)
      HA_BASE_URL: ${HA_BASE_URL:-}
      HA_ACCESS_TOKEN: ${HA_ACCESS_TOKEN:-}
      # Optional tuning
      HA_DOMAIN_ALLOWLIST: ${HA_DOMAIN_ALLOWLIST:-}
      HA_POLL_INTERVAL_S: ${HA_POLL_INTERVAL_S:-60}
      HA_CHECKPOINT_OVERLAP_S: ${HA_CHECKPOINT_OVERLAP_S:-30}
      HA_WS_PING_INTERVAL_S: ${HA_WS_PING_INTERVAL_S:-30}
      HA_WS_PONG_TIMEOUT_S: ${HA_WS_PONG_TIMEOUT_S:-10}
      HA_DISCRETION_TIMEOUT_S: ${HA_DISCRETION_TIMEOUT_S:-5}
      HA_EVENT_QUEUE_MAX: ${HA_EVENT_QUEUE_MAX:-100}
    volumes:
      - ./logs:/app/logs
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:40087/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  # ── Layer 2: OAuth Gate ───────────────────────────────────────────────

  oauth-gate:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["uv", "run", "--frozen", "--no-dev", "python", "scripts/oauth_gate.py"]
    command: []
    environment:
      <<: *postgres-env
      POSTGRES_DB: ${POSTGRES_DB:-butlers}
      OAUTH_GATE_TIMEOUT: ${OAUTH_GATE_TIMEOUT:-0}
      OAUTH_POLL_INTERVAL: ${OAUTH_POLL_INTERVAL:-5}
      SKIP_OAUTH_CHECK: ${SKIP_OAUTH_CHECK:-false}
    networks: [db]
    depends_on:
      dashboard-api:
        condition: service_healthy

  # ── Layer 3: Butler Backend + Gmail ───────────────────────────────────
  # butlers-up starts ALL butler daemons (switchboard, general, relationship,
  # health, etc.) in a single process via `butlers up`. Each butler's MCP
  # server listens on its configured port (switchboard on 41100, etc.).
  # LLM runtime CLIs are spawned as subprocesses.
  butlers-up:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "butlers/up", "sh", "-c", "uv sync --dev && exec uv run butlers up"]
    command: []
    ports:
      - "127.0.0.1:${SWITCHBOARD_HOST_PORT:-41100}:41100"
    # All Butler MCP listeners use 41100-41111, which overlaps Linux's
    # default ephemeral range. Exclude it from outbound source-port selection
    # so a database connection cannot race a later listener bind at startup.
    sysctls:
      net.ipv4.ip_local_reserved_ports: "41100-41111"
    # Allow bubblewrap (bwrap) sandbox inside the container — Codex CLI uses
    # bwrap for command isolation, which requires user-namespace creation.
    # Docker's default AppArmor profile blocks this; unconfined lifts that
    # restriction while the bwrap sandbox itself remains fully functional.
    security_opt:
      - apparmor:unconfined
    environment:
      <<: *postgres-env
      BUTLERS_SWITCHBOARD_URL: http://dashboard-api:41200
      # Owner-facing alerts are generated here, not in dashboard-api.  Preserve
      # the configured public frontend origin (including any path prefix) so a
      # Telegram deep link never falls back to the daemon host's localhost.
      DASHBOARD_URL: ${DASHBOARD_URL:-${OAUTH_DASHBOARD_URL:-http://localhost:41200}}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      HOME: /root
      # The Messenger butler's whatsapp module sends over the bridge the
      # connector-whatsapp-user service owns (bu-0c69e) — it never spawns its
      # own. Point it at the shared wa_bridge_socket volume, matching the
      # connector's WA_BRIDGE_SOCKET, so only one authenticated session exists.
      WHATSAPP_BRIDGE_SOCKET: /tmp/wa-bridge/bridge.sock
    volumes:
      - ./roster:/app/roster:ro
      - ./logs:/app/logs
      - wa_bridge_socket:/tmp/wa-bridge
      - runtime_claude:/root/.claude
      - runtime_codex:/root/.codex
      - runtime_opencode:/root/.opencode
      - runtime_gemini:/root/.gemini
      # bu-ckkpz.4: read-only mount of ONLY the bd JSONL export (never the
      # whole .beads/ dir -- that also holds .beads-credential-key, which
      # must never enter a container). Feeds Switchboard's
      # decision-review-digest / decision-escalation-check jobs; see
      # butlers.jobs.decision_review's module docstring for why this is the
      # only path from beads issue data into the running system today.
      - ./.beads/issues.export.jsonl:/app/.beads/issues.export.jsonl:ro
    # bu-0uqgo.11 (REQ-core-credentials-002): Switchboard verifies capabilities
    # here, so all-butlers receives the public keyring and deliberately declares
    # no `secrets:` at all -- there is no private file for this stack to hold.
    configs:
      - source: runtime_probe_control_verifiers
        target: /run/secrets/runtime_probe_control_verifiers
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:41100/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 120s
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      oauth-gate:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
    restart: unless-stopped

  connector-gmail:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/gmail", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.gmail"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: gmail
      CONNECTOR_CHANNEL: email
      CONNECTOR_BUTLER_DB_NAME: ${POSTGRES_DB:-butlers}
      CONNECTOR_BUTLER_DB_SCHEMA: shared
      BUTLER_SHARED_DB_NAME: ${POSTGRES_DB:-butlers}
      BUTLER_SHARED_DB_SCHEMA: shared
    volumes:
      - ./logs:/app/logs
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      oauth-gate:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    restart: unless-stopped

  connector-google-drive:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/google_drive", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.google_drive"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: google_drive
      CONNECTOR_CHANNEL: google_drive
      CONNECTOR_HEALTH_PORT: "40088"
      CONNECTOR_BUTLER_DB_NAME: ${POSTGRES_DB:-butlers}
      BUTLER_SHARED_DB_NAME: ${POSTGRES_DB:-butlers}
      BUTLER_SHARED_DB_SCHEMA: shared
      GDRIVE_POLL_INTERVAL_S: ${GDRIVE_POLL_INTERVAL_S:-300}
      GDRIVE_BATCH_WINDOW_S: ${GDRIVE_BATCH_WINDOW_S:-7200}
      GDRIVE_ACCOUNT_RESCAN_INTERVAL_S: ${GDRIVE_ACCOUNT_RESCAN_INTERVAL_S:-300}
    volumes:
      - ./logs:/app/logs
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      oauth-gate:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:40088/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  connector-google-health:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    entrypoint: ["/app/scripts/dev_entrypoint.sh", "connectors/google-health", "uv", "run", "--frozen", "--no-dev", "python", "-m", "butlers.connectors.google_health"]
    command: []
    environment:
      <<: *connector-env
      CONNECTOR_PROVIDER: google_health
      CONNECTOR_CHANNEL: wellness
      CONNECTOR_HEALTH_PORT: "40090"
    volumes:
      - ./logs:/app/logs
    networks: [db, backend, egress]
    depends_on:
      log-init:
        condition: service_completed_successfully
      migrations:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
      oauth-gate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:40090/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  # ── Hotreload profile ─────────────────────────────────────────────────
  # Volume-mount source for live code changes. Use --scale butlers-up=0
  # --scale dashboard-api=0 to prevent conflicts (handled by dev-compose.sh).

  butlers-up-hotreload:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    profiles: [hotreload]
    entrypoint: ["sh", "-c", "uv sync --dev && exec uv run butlers up"]
    command: []
    # Keep the same MCP port reservation as the baked daemon service above.
    sysctls:
      net.ipv4.ip_local_reserved_ports: "41100-41111"
    security_opt:
      - apparmor:unconfined
    environment:
      <<: *postgres-env
      BUTLERS_SWITCHBOARD_URL: http://dashboard-api:41200
      DASHBOARD_URL: ${DASHBOARD_URL:-${OAUTH_DASHBOARD_URL:-http://localhost:41200}}
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      HOME: /root
    volumes:
      - ./src:/app/src
      - ./roster:/app/roster:ro
      - ./pyproject.toml:/app/pyproject.toml:ro
      - ./logs:/app/logs
      - uv_cache:/root/.cache/uv
      - runtime_claude:/root/.claude
      - runtime_codex:/root/.codex
      - runtime_opencode:/root/.opencode
      - runtime_gemini:/root/.gemini
      # bu-ckkpz.4: see butlers-up's own volumes block above for why only
      # this one file is mounted, never the whole .beads/ dir.
      - ./.beads/issues.export.jsonl:/app/.beads/issues.export.jsonl:ro
    # bu-0uqgo.11 (REQ-core-credentials-002): Switchboard verifies capabilities
    # here, so all-butlers receives the public keyring and deliberately declares
    # no `secrets:` at all -- there is no private file for this stack to hold.
    configs:
      - source: runtime_probe_control_verifiers
        target: /run/secrets/runtime_probe_control_verifiers
    networks:
      db:
      backend:
        aliases:
          - butlers-up
      egress:
    depends_on:
      log-init:
        condition: service_completed_successfully
      oauth-gate:
        condition: service_completed_successfully
      butlers-up:
        condition: service_healthy
    restart: unless-stopped

  dashboard-api-hotreload:
    image: butlers-app:${BUTLERS_APP_TAG:-latest}
    profiles: [hotreload]
    entrypoint: ["sh", "-c", "uv sync --dev && exec uv run butlers dashboard --host 0.0.0.0 --port 41200"]
    command: []
    ports:
      - "127.0.0.1:${DASHBOARD_HOST_PORT:-41200}:41200"
    environment:
      <<: *postgres-env
      GOOGLE_OAUTH_REDIRECT_URI: ${GOOGLE_OAUTH_REDIRECT_URI:-http://localhost:41200/api/oauth/google/callback}
      OAUTH_DASHBOARD_URL: ${OAUTH_DASHBOARD_URL:-}
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:-}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET:-}
      SPOTIFY_OAUTH_REDIRECT_URI: ${SPOTIFY_OAUTH_REDIRECT_URI:-}
      OWNTRACKS_CONNECTOR_HOST: ${OWNTRACKS_CONNECTOR_HOST:-}
      OWNTRACKS_CONNECTOR_PORT: ${OWNTRACKS_CONNECTOR_PORT:-}
      OWNTRACKS_WEBHOOK_BASE_PATH: ${OWNTRACKS_WEBHOOK_BASE_PATH:-/owntracks}
      BUTLERS_DISABLE_FILE_LOGGING: "1"
      BUTLERS_HOST: butlers-up
      WHATSAPP_BRIDGE_SOCKET: /tmp/wa-bridge/bridge.sock
      BUTLERS_BACKUP_DIR: /backups
      BUTLERS_API_DB_POOL_MIN_SIZE: ${BUTLERS_API_DB_POOL_MIN_SIZE:-0}
      BUTLERS_API_DB_POOL_MAX_SIZE: ${BUTLERS_API_DB_POOL_MAX_SIZE:-2}
      DASHBOARD_API_KEY: ${DASHBOARD_API_KEY:-}
      DASHBOARD_AUTH_DB_USER: ${DASHBOARD_AUTH_DB_USER:-}
      DASHBOARD_AUTH_DB_PASSWORD: ${DASHBOARD_AUTH_DB_PASSWORD:-}
      DASHBOARD_AUTH_ORIGIN: ${DASHBOARD_AUTH_ORIGIN:-}
      DASHBOARD_AUTH_RP_ID: ${DASHBOARD_AUTH_RP_ID:-}
      DASHBOARD_AUTH_DEPLOYMENT: ${DASHBOARD_AUTH_DEPLOYMENT:-default}
      DASHBOARD_AUTH_TRUSTED_PROXY_PEERS: ${DASHBOARD_AUTH_TRUSTED_PROXY_PEERS:-}
      # bu-g4jg1: see the dashboard-api service above for why this passthrough
      # is required for butlers.jobs.external_deadman to see the URL at all.
      EXTERNAL_DEADMAN_URL: ${EXTERNAL_DEADMAN_URL:-}
    volumes:
      - ./src:/app/src
      - ./roster:/app/roster:ro
      - ./pyproject.toml:/app/pyproject.toml:ro
      - ./logs:/app/logs
      - uv_cache:/root/.cache/uv
      - wa_bridge_socket:/tmp/wa-bridge
      - butlers_backups:/backups:ro
      # bu-hmdqz.6: see dashboard-api's own volumes block above for why.
      - ./.beads/issues.export.jsonl:/app/.beads/issues.export.jsonl:ro
    # bu-0uqgo.11 (REQ-core-credentials-002): Dashboard is the only signing
    # side, so it is the only service that receives the private signer.  The
    # keyring below is public and shared with every verifying process; the
    # signer is not shared with anything.  Both are read once at process
    # start, so rotation is a restart, never a live file swap.
    secrets:
      - source: runtime_probe_control_signing_key
        target: runtime_probe_control_signing_key
        mode: 0400
    configs:
      - source: runtime_probe_control_verifiers
        target: /run/secrets/runtime_probe_control_verifiers
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:41200/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 120s
    # Match dashboard-api exactly: this is the only hotreload runtime that
    # may launch a Dashboard CLI-auth child through Bubblewrap.
    security_opt:
      - apparmor:unconfined
      - systempaths=unconfined
      - seccomp:./deploy/seccomp/dashboard-runtime-cli-sandbox.json
    depends_on:
      migrations:
        condition: service_completed_successfully
    networks:
      db:
      backend:
        aliases:
          - dashboard-api
      frontend:
        aliases:
          - dashboard-api
      egress:
    restart: unless-stopped

# ── Runtime-probe control keys ──────────────────────────────────────────
# REQ-core-credentials-002.  A host path is configuration, not key material;
# neither document is ever committed.  Both default to a tracked placeholder
# that is deliberately not a key, so the canonical launcher still performs one
# ordinary full-stack up on a machine that has not provisioned them -- and the
# plane stays closed while it has not: the signer placeholder is rejected on
# its mode and its field set, the keyring placeholder on its field set, so
# Dashboard signs nothing and Switchboard answers 503/unavailable.
# Provisioning and rotation: docs/operations/runtime-probe-control-keys.md.

secrets:
  runtime_probe_control_signing_key:
    file: "${RUNTIME_PROBE_CONTROL_SIGNING_KEY_FILE:-./deploy/runtime-probe-control/signing-key-unprovisioned.json}"

configs:
  # Public keys only. Every verifying process reads this; nobody but the
  # provisioning operator may write it, since whoever writes it chooses which
  # keys Switchboard will accept.
  runtime_probe_control_verifiers:
    file: "${RUNTIME_PROBE_CONTROL_VERIFIERS_FILE:-./deploy/runtime-probe-control/verifiers-unprovisioned.json}"

# ── Volumes ─────────────────────────────────────────────────────────────

volumes:
  minio_data:
  frontend_node_modules:
  uv_cache:
  runtime_claude:
  runtime_codex:
  runtime_opencode:
  runtime_gemini:
  wa_bridge_socket:
  butlers_backups:

# ── Networks ────────────────────────────────────────────────────────────

networks:
  # NOTE: The ordinary application stack intentionally uses no `internal: true`
  # network: Docker's DOCKER-ISOLATION-STAGE-1 rules for multiple internal
  # networks interfere with its normal inter-container communication. The
  # protected restore-drill fragment is the deliberate exception: its
  # credentialed executor has one internal-only relay network, while the relay
  # egress bridge remains fenced by scripts/restore-drill-firewall.sh.
  db:
    driver: bridge
  backend:
    driver: bridge
  frontend:
    driver: bridge
  egress:
    # Services on this network can reach the internet (for LLM APIs, OAuth, etc.).
    # Private subnets blocked by scripts/egress-firewall.sh.
    driver: bridge
