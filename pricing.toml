# Per-model token pricing (USD per token)
# Used by dashboard cost estimation
#
# Prices sourced from vendor public pricing pages.
# Update this file when new models are released or prices change.
#
# Cache pricing fields (both optional, per model or per tier):
#   cached_input_price_per_token   — prompt-cache READS
#   cache_creation_price_per_token — prompt-cache WRITES
# When absent, the bucket bills at the full input rate (conservative
# fallback for vendors that don't discount cached tokens or whose
# discount is unknown).
#
# billing_class is optional for metered models. Declare `subscription` or
# `local` for known zero-marginal usage so it cannot be confused with a model
# missing from this file.
#
# Anthropic: cache reads = 0.1x input, 5-minute cache writes = 1.25x input.

[models]

[models."claude-sonnet-4-5-20250929"]
input_price_per_token = 0.000003    # $3 / 1M tokens
cached_input_price_per_token = 0.0000003   # $0.30 / 1M tokens (0.1x input)
cache_creation_price_per_token = 0.00000375  # $3.75 / 1M tokens (1.25x input)
output_price_per_token = 0.000015   # $15 / 1M tokens

[models."claude-sonnet-4-6"]
input_price_per_token = 0.000003    # $3 / 1M tokens
cached_input_price_per_token = 0.0000003   # $0.30 / 1M tokens (0.1x input)
cache_creation_price_per_token = 0.00000375  # $3.75 / 1M tokens (1.25x input)
output_price_per_token = 0.000015   # $15 / 1M tokens

[models."claude-haiku-4-5-20251001"]
input_price_per_token = 0.000001    # $1 / 1M tokens
cached_input_price_per_token = 0.0000001   # $0.10 / 1M tokens (0.1x input)
cache_creation_price_per_token = 0.00000125  # $1.25 / 1M tokens (1.25x input)
output_price_per_token = 0.000005   # $5 / 1M tokens

[models."claude-opus-4-6"]
input_price_per_token = 0.000005    # $5 / 1M tokens
cached_input_price_per_token = 0.0000005   # $0.50 / 1M tokens (0.1x input)
cache_creation_price_per_token = 0.00000625  # $6.25 / 1M tokens (1.25x input)
output_price_per_token = 0.000025   # $25 / 1M tokens

# Google — sourced from https://cloud.google.com/vertex-ai/generative-ai/pricing (2026-03-23)

[models."gemini-2.5-pro"]
input_price_per_token = 0.00000125  # $1.25 / 1M tokens (≤200K context)
output_price_per_token = 0.00001    # $10.00 / 1M tokens

[models."gemini-2.5-flash"]
input_price_per_token = 0.0000003   # $0.30 / 1M tokens (≤200K context)
output_price_per_token = 0.0000025  # $2.50 / 1M tokens

# OpenAI — sourced from https://developers.openai.com/api/docs/pricing/ (2026-04-28)
# gpt-5.5 and gpt-5.4 have two pricing tiers based on context length; entries below use the <272K tier.
# gpt-5.5 (>272K): $10.00/$45.00 per 1M tokens (cached input $1.00)
# gpt-5.4 (>272K): $5.00/$22.50 per 1M tokens
# gpt-5.4-pro (>272K): $60.00/$270.00 per 1M tokens
# gpt-5.5-pro has no long-context tier.

# GPT-5.6 estimates use OpenAI Standard API-equivalent token prices, independent
# of the active Codex subscription, so the dashboard remains a general usage
# heuristic. This deployment assumes every request is <=272K, so entries are
# deliberately flat rather than context-tiered.
# Source: https://developers.openai.com/api/docs/pricing (2026-08-01).
[models."gpt-5.6-sol"]
billing_class = "metered"
input_price_per_token = 0.000005  # $5.00 / 1M tokens (≤272K context)
cached_input_price_per_token = 0.0000005  # $0.50 / 1M tokens
cache_creation_price_per_token = 0.00000625  # $6.25 / 1M tokens
output_price_per_token = 0.00003  # $30.00 / 1M tokens

[models."gpt-5.6-terra"]
billing_class = "metered"
input_price_per_token = 0.000002  # $2.00 / 1M tokens (≤272K context)
cached_input_price_per_token = 0.0000002  # $0.20 / 1M tokens
cache_creation_price_per_token = 0.0000025  # $2.50 / 1M tokens
output_price_per_token = 0.000012  # $12.00 / 1M tokens

[models."gpt-5.6-luna"]
billing_class = "metered"
input_price_per_token = 0.0000002  # $0.20 / 1M tokens (≤272K context)
cached_input_price_per_token = 0.00000002  # $0.02 / 1M tokens
cache_creation_price_per_token = 0.00000025  # $0.25 / 1M tokens
output_price_per_token = 0.0000012  # $1.20 / 1M tokens

# The catalog uses these effort-suffixed Luna IDs directly, so each requires
# an explicit pricing key even though the API-equivalent rates match base Luna.
[models."gpt-5.6-luna-high"]
billing_class = "metered"
input_price_per_token = 0.0000002
cached_input_price_per_token = 0.00000002
cache_creation_price_per_token = 0.00000025
output_price_per_token = 0.0000012

[models."gpt-5.6-luna-xhigh"]
billing_class = "metered"
input_price_per_token = 0.0000002
cached_input_price_per_token = 0.00000002
cache_creation_price_per_token = 0.00000025
output_price_per_token = 0.0000012

[models."gpt-5.5"]
input_price_per_token = 0.000005    # $5.00 / 1M tokens (<272K context)
cached_input_price_per_token = 0.0000005  # $0.50 / 1M tokens (<272K context)
output_price_per_token = 0.00003    # $30.00 / 1M tokens (<272K context)

[models."gpt-5.5-pro"]
input_price_per_token = 0.00003     # $30.00 / 1M tokens
output_price_per_token = 0.00018    # $180.00 / 1M tokens

[models."gpt-5.4"]
input_price_per_token = 0.0000025   # $2.50 / 1M tokens (<272K context)
cached_input_price_per_token = 0.00000025  # $0.25 / 1M tokens (assumed 10% of input, matching gpt-5.5 / gpt-5.4-mini)
output_price_per_token = 0.000015   # $15.00 / 1M tokens (<272K context)

[models."gpt-5.4-pro"]
input_price_per_token = 0.00003     # $30.00 / 1M tokens (<272K context)
output_price_per_token = 0.00018    # $180.00 / 1M tokens (<272K context)

[models."gpt-5.4-mini"]
[[models."gpt-5.4-mini".tiers]]
context_threshold = 0
input_price_per_token = 0.00000075   # $0.75 / 1M tokens
cached_input_price_per_token = 0.000000075  # $0.075 / 1M tokens
output_price_per_token = 0.0000045   # $4.50 / 1M tokens

[models."gpt-5.3-codex"]
input_price_per_token = 0.00000175  # $1.75 / 1M tokens
cached_input_price_per_token = 0.000000175  # $0.175 / 1M tokens (assumed 10% of input)
output_price_per_token = 0.000014   # $14.00 / 1M tokens

[models."gpt-5.3-codex-spark"]
input_price_per_token = 0.00000175  # $1.75 / 1M tokens (assumed same as gpt-5.3-codex)
cached_input_price_per_token = 0.000000175  # $0.175 / 1M tokens (assumed 10% of input)
output_price_per_token = 0.000014   # $14.00 / 1M tokens (assumed same as gpt-5.3-codex)

[models."gpt-5.2"]
input_price_per_token = 0.00000175  # $1.75 / 1M tokens
cached_input_price_per_token = 0.000000175  # $0.175 / 1M tokens (assumed 10% of input)
output_price_per_token = 0.000014   # $14.00 / 1M tokens

[models."gpt-5.1"]
input_price_per_token = 0.00000125  # $1.25 / 1M tokens
cached_input_price_per_token = 0.000000125  # $0.125 / 1M tokens (10% of input)
output_price_per_token = 0.00001    # $10.00 / 1M tokens

[models."gpt-5"]
input_price_per_token = 0.00000125  # $1.25 / 1M tokens
cached_input_price_per_token = 0.000000125  # $0.125 / 1M tokens (10% of input)
output_price_per_token = 0.00001    # $10.00 / 1M tokens

[models."gpt-5-mini"]
input_price_per_token = 0.00000025  # $0.25 / 1M tokens
cached_input_price_per_token = 0.000000025  # $0.025 / 1M tokens (10% of input)
output_price_per_token = 0.000002   # $2.00 / 1M tokens

[models."gpt-5-nano"]
input_price_per_token = 0.00000005  # $0.05 / 1M tokens
cached_input_price_per_token = 0.000000005  # $0.005 / 1M tokens (10% of input)
output_price_per_token = 0.0000004  # $0.40 / 1M tokens

# OpenCode Zen — sourced from https://opencode.ai/docs/zen/ (2026-04-24)

[models."opencode-go/big-pickle"]
input_price_per_token = 0.0
output_price_per_token = 0.0

[models."opencode-go/minimax-m2.5-free"]
input_price_per_token = 0.0
output_price_per_token = 0.0

[models."opencode-go/ling-2.6-flash-free"]
input_price_per_token = 0.0
output_price_per_token = 0.0

[models."opencode-go/hy3-preview-free"]
input_price_per_token = 0.0
output_price_per_token = 0.0

[models."opencode-go/nemotron-3-super-free"]
input_price_per_token = 0.0
output_price_per_token = 0.0

[models."opencode-go/minimax-m2.5"]
input_price_per_token = 0.0000003   # $0.30 / 1M tokens
output_price_per_token = 0.0000012  # $1.20 / 1M tokens

[models."opencode-go/minimax-m2.7"]
input_price_per_token = 0.0000003   # $0.30 / 1M tokens
output_price_per_token = 0.0000012  # $1.20 / 1M tokens

[models."opencode-go/glm-5.1"]
input_price_per_token = 0.0000014   # $1.40 / 1M tokens
output_price_per_token = 0.0000044  # $4.40 / 1M tokens

[models."opencode-go/glm-5"]
input_price_per_token = 0.000001    # $1.00 / 1M tokens
output_price_per_token = 0.0000032  # $3.20 / 1M tokens

[models."opencode-go/kimi-k2.5"]
input_price_per_token = 0.0000006   # $0.60 / 1M tokens
output_price_per_token = 0.000003   # $3.00 / 1M tokens

[models."opencode-go/kimi-k2.6"]
input_price_per_token = 0.00000095  # $0.95 / 1M tokens
cached_input_price_per_token = 0.000000095  # $0.095 / 1M tokens (assumed 10% of input, matching the qwen3.6/3.7-plus cache discount reconciled from the same price sheet)
output_price_per_token = 0.000004   # $4.00 / 1M tokens

# Prices reconciled against the OpenCode Go price sheet (2026-07-05).

[models."opencode-go/glm-5.2"]
input_price_per_token = 0.0000014   # $1.40 / 1M tokens
output_price_per_token = 0.0000044  # $4.40 / 1M tokens

[models."opencode-go/kimi-k2.7-code"]
input_price_per_token = 0.00000095  # $0.95 / 1M tokens
output_price_per_token = 0.000004   # $4.00 / 1M tokens

[models."opencode-go/mimo-v2.5"]
input_price_per_token = 0.00000014  # $0.14 / 1M tokens
cached_input_price_per_token = 0.000000014  # $0.014 / 1M tokens (assumed 10% of input, matching the qwen3.6/3.7-plus cache discount reconciled from the same price sheet)
output_price_per_token = 0.00000028  # $0.28 / 1M tokens

[models."opencode-go/mimo-v2.5-pro"]
input_price_per_token = 0.00000174  # $1.74 / 1M tokens
output_price_per_token = 0.00000348  # $3.48 / 1M tokens

[models."opencode-go/minimax-m3"]
input_price_per_token = 0.0000003   # $0.30 / 1M tokens
output_price_per_token = 0.0000012  # $1.20 / 1M tokens

# Qwen3.7 Plus — context-tiered at 256K tokens.
[[models."opencode-go/qwen3.7-plus".tiers]]
context_threshold = 0               # <= 256K tokens
input_price_per_token = 0.0000004   # $0.40 / 1M tokens
output_price_per_token = 0.0000016  # $1.60 / 1M tokens
cached_input_price_per_token = 0.00000004  # $0.04 / 1M tokens
[[models."opencode-go/qwen3.7-plus".tiers]]
context_threshold = 256000          # > 256K tokens
input_price_per_token = 0.0000012   # $1.20 / 1M tokens
output_price_per_token = 0.0000048  # $4.80 / 1M tokens
cached_input_price_per_token = 0.00000012  # $0.12 / 1M tokens

[models."opencode-go/qwen3.7-max"]
input_price_per_token = 0.0000025   # $2.50 / 1M tokens
output_price_per_token = 0.0000075  # $7.50 / 1M tokens

# Qwen3.6 Plus — context-tiered at 256K tokens.
[[models."opencode-go/qwen3.6-plus".tiers]]
context_threshold = 0               # <= 256K tokens
input_price_per_token = 0.0000005   # $0.50 / 1M tokens
output_price_per_token = 0.000003   # $3.00 / 1M tokens
cached_input_price_per_token = 0.00000005  # $0.05 / 1M tokens
[[models."opencode-go/qwen3.6-plus".tiers]]
context_threshold = 256000          # > 256K tokens
input_price_per_token = 0.000002    # $2.00 / 1M tokens
output_price_per_token = 0.000006   # $6.00 / 1M tokens
cached_input_price_per_token = 0.0000002  # $0.20 / 1M tokens

[models."opencode-go/qwen3.5-plus"]
input_price_per_token = 0.0000002   # $0.20 / 1M tokens
output_price_per_token = 0.0000012  # $1.20 / 1M tokens

[models."opencode-go/deepseek-v4-pro"]
input_price_per_token = 0.00000174  # $1.74 / 1M tokens (sourced from OpenCode Go price sheet, 2026-07-05)
output_price_per_token = 0.00000348  # $3.48 / 1M tokens

[models."opencode-go/deepseek-v4-flash"]
input_price_per_token = 0.00000014  # $0.14 / 1M tokens (sourced from OpenCode Go price sheet, 2026-07-05)
output_price_per_token = 0.00000028  # $0.28 / 1M tokens

# Ollama (local/self-hosted) — zero cost
# Model IDs match the ollama/ prefix used in model_catalog_defaults.toml
# billing_class = "local" so a known zero marginal cost is never confused
# with a metered model missing from this file (bu-2jtfw.4).

[models."ollama/qwen2.5-coder:7b"]
input_price_per_token = 0.0
output_price_per_token = 0.0
billing_class = "local"

[models."ollama/llama3.3:latest"]
input_price_per_token = 0.0
output_price_per_token = 0.0
billing_class = "local"

[models."ollama/deepseek-r1:32b"]
input_price_per_token = 0.0
output_price_per_token = 0.0
billing_class = "local"

[models."ollama/qwen3.5:9b"]
input_price_per_token = 0.0
output_price_per_token = 0.0
billing_class = "local"
