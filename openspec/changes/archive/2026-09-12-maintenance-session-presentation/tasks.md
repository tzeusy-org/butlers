## 1. Presentation contract

- [x] 1.1 Add failing API-boundary tests for the exact machine-class taxonomy,
      legacy heartbeat projection, and safe owner fallback.
- [x] 1.2 Add the typed `machine_class` Timeline contract and pure
      structured-trigger classifier without changing persistence or episode
      exclusion behavior.

## 2. Owner and Internal lenses

- [x] 2.1 Add failing Timeline ledger/page tests for the default owner lens,
      URL-backed accessible Internal control, and expandable per-butler
      maintenance rollups.
- [x] 2.2 Implement Timeline maintenance filtering/rollups and the
      keyboard-operable Internal control.
- [x] 2.3 Add failing Dashboard Now tests for owner-focused default behavior,
      failed-maintenance visibility, and Internal per-butler rollups.
- [x] 2.4 Implement Dashboard Now machine-class consumption and its
      URL-backed accessible Internal control.
- [x] 2.5 Correct the rendered Dashboard Now failure treatment so an exact
      `success === false` has a visible failure marker in both the owner and
      Internal lenses, including the Internal failed-run count.

## 3. Verification

- [x] 3.1 Run focused backend and frontend regression tests, lint, format,
      build, and strict OpenSpec validation.
- [x] 3.2 Review the final diff against the non-goals and update task status.
