## 1. Durable representation and target acceptance

- [ ] 1.1 Add a Switchboard-owned, per-target delivery-intent schema with immutable event/target/segment identity, payload retention, closed state/reason vocabulary, fence, server timestamps, retry budget, and narrow runtime grants. Add migrated real-PostgreSQL tests for uniqueness, invalid states, concurrent claims, lease succession, and retention. Covers `REQ-ingestion-target-delivery-recovery-001`, `-003`, and `-008`.
- [ ] 1.2 Add atomic target `route_inbox` acceptance by stable delivery key, immutable payload digest, target binding, and same-key receipt lookup under the existing trusted-host `trusted_route_callers` boundary, preserving the dashboard claim path. Prove concurrent duplicate calls and lost-response recovery create one inbox row and at most one target session; both duplicate acceptance and read-only receipt lookup must return conflict on same-key/different-payload or target mismatch without a false receipt, on a real migrated database. Covers `REQ-ingestion-target-delivery-recovery-002` and `REQ-butler-switchboard-003`.

## 2. Classification and dispatch cutover

- [ ] 2.1 Make non-dashboard classifier `route_to_butler` calls collect validated decisions without target side effects; include triage bypass and no-tool fallback in the same complete-plan admission. Commit the already accepted source event's classified outcome, plan, and every intent before dispatch; prove transaction failure makes zero target calls and duplicate ingress reuses the original plan. Preserve dashboard lane behavior. Covers `REQ-module-pipeline-001` and `REQ-ingestion-target-delivery-recovery-001`.
- [ ] 2.2 Persist ordinary conversation-decomposition output and stable per-concept segment IDs with all intents before fan-out; update outcomes afterward without rewriting the classified plan. Keep `dispatch_outcomes` as a per-butler compatibility aggregate that cannot hide an unresolved same-butler segment, and set parent `lifecycle_state='routed'` only when all ordinary segments have target receipts. Reconcile with the active `repair-whatsapp-identity-reconciliation` MODIFIED fan-out block before either change archives, preserve calendar proposal special handling, and test two distinct concepts for one target. Covers `REQ-conversation-decomposition-002`, `REQ-module-pipeline-002`, and `REQ-ingestion-target-delivery-recovery-001`.
- [ ] 2.3 Implement one fenced Switchboard intent dispatcher for immediate post-commit delivery and crash recovery. Use canonical route transport certainty, bounded backoff and age/attempt limits, current-policy recheck, same-key receipt lookup, and terminal `accepted` ownership. Inject not-attempted, policy denied, rejected, uncertain, worker death, and stale-fence cases; prove no automatic resend after uncertainty. Covers `REQ-ingestion-target-delivery-recovery-003` and `-004`.

## 3. Ingestion truth and owner controls

- [ ] 3.1 Derive per-target and event-level delivery API projections from intents/receipts, including partial fan-out and unavailable evidence; keep coarse source `ingested` distinct from target completion. Replace every `public.ingestion_events` replay branch with the status matrix in Decision 5: `failed`, `ingested`, and `replay_failed` use only original safe intents; `replay_pending` is idempotent only with actual queued work; other statuses conflict. Never reset terminal `message_inbox` or reclassify from recovery. Reconcile legacy orphan pending markers without reporting false queued work. Retain connector `filtered_events` replay-safe policy joins and email semantics. Test each status, accepted-plus-waiting, ambiguous, rejected, legacy missing-plan, duplicate request, and store failure. Covers `REQ-ingestion-target-delivery-recovery-005`, `-006`, and `REQ-ingestion-event-registry-002`.
- [ ] 3.2 Update `/ingestion` Timeline row, drawer, and recovery controls to display waiting/accepted/ambiguous/terminal states and immediate action feedback, using the existing dot-and-word status visual language. Prove keyboard and screen-reader access, repeat-safe requests, cached-row failure behavior, and no false all-clear. Covers `REQ-dashboard-ingestion-dispatch-console-001` and `-002`.

## 4. Historical recovery gate

- [ ] 4.1 Build a read-only content-blind historical dry run over retained event, inbox, transport, and target receipt evidence. Return grouped counts and an exact privately reviewable candidate key set; exclude missing payloads, accepted, ambiguous, rejected, policy-denied, pruned, and otherwise unprovable rows. Prove an empty eligible set is valid and no source cursor or connector queue changes. Covers `REQ-ingestion-target-delivery-recovery-007` and `-008`.
- [ ] 4.2 Add an owner-gated exact-key historical admission operation that requires bounded age/channel policy, rechecks evidence at admission, and queues only approved still-eligible keys. Test changed evidence, duplicate approval, stale receipt, and partial exclusions without recording message content in audit/telemetry. Historical admission remains inactive until the owner reviews the dry run and approves the exact set. Covers `REQ-ingestion-target-delivery-recovery-007`.

## 5. Cross-boundary verification and documentation

- [ ] 5.1 Extend behavior-executing tests at the real Switchboard-to-target seam, including route transport fault injection and real PostgreSQL acceptance, through more than one retry/lease window. Verify dashboard turns, Messenger, domain-event subscribers, explicit misroute correction, connector ingress replay, and email replay-safe policy remain governed by their existing contracts. Cite each new requirement ID at its nearest owning gate; avoid duplicate source-scan tests for behavior already exercised.
- [ ] 5.2 Update RFC 0003 and relevant ingestion/route topology and operator documentation to describe plan-before-call, stable acceptance receipts, target versus session ownership, safe retry, and the historical gate. Reconcile the new specs with active changes and run strict OpenSpec validation, spec-overwrite guard, authoring trace check, applicable targeted tests, collection where topology changes, and terminal hosted CI on the implementation head.
