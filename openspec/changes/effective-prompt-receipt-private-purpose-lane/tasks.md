## 1. Effective prompt receipt

- [ ] 1.1 Add deterministic effective-prompt digest/provenance composition with bounded source identifiers and explicit unavailable/shadowed states.
- [ ] 1.2 Add guarded nullable session columns and persist the exact receipt before runtime invocation without changing composition behavior.
- [ ] 1.3 Add the protected cross-butler prompt-detail API with corrupt-receipt and degraded-source truth.
- [ ] 1.4 Extend focused composer, session, migration, and API tests with synthetic prompt content only.

## 2. Roster drift and dashboard doors

- [ ] 2.1 Compare current roster-source receipts with the latest executed receipt and project `matches_git`, `drifted`, or `unknown` plus bounded changed sources.
- [ ] 2.2 Render effective prompt disclosure/provenance on session detail and composed preview/drift state on butler Configuration without changing prompt authoring.
- [ ] 2.3 Render `purpose_lane` on relevant session/spend rows with accessible, established badge semantics.

## 3. Private-content routing

- [ ] 3.1 Derive the closed purpose lane from trusted channel context without inspecting prompt or message content.
- [ ] 3.2 Enforce local-only selection/failover for private content and require a current explicitly targeted audited spend-rule exception for remote selection.
- [ ] 3.3 Record bounded refusal/override evidence and prove remote-only refusal occurs before provider setup or invocation.

## 4. Documentation and verification

- [ ] 4.1 Update session lifecycle, daemon composition, and model-routing documentation.
- [ ] 4.2 Run focused backend/frontend tests, migration/schema gates, OpenSpec strict/overwrite checks, frontend lint/knip/build/test, planner, guards, and diff hygiene.
