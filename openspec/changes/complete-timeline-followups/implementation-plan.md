# Proposed implementation packets

These are fully shaped handoffs, not dispatched or approved Beads. Materialize them only after owner approval of the exact proposal; preserve the dependencies below and map symbolic keys to the resulting IDs.

## density: Timeline server-counted minute density and historical seek

Type: task; priority: 3; status after creation: blocked; labels: complexity:high, packet-complete, spec-approval-pending.

Dependencies: none; approval: exact-artifact approval; parent bu-ddo0n.

### Description

Implement the approved density-and-seek contract as one vertical outcome: select a busy minute beyond the loaded page and load all its matching events without page-boundary count errors. No client-derived aggregate, local search, attention strip or shortcut redesign.

### Design

PROPOSED, NOT ADOPTED: openspec/changes/complete-timeline-followups/. Owner approval of exact artifact revision required before dispatch. Baseline 66ed58f7f7e80963d449500141ed4cb069209967. GOVERNING REQUIREMENT: REQ-dashboard-visibility-001. SURFACES: src/butlers/api/read_models/timeline_v1.py optional interval filters/shared source predicates and aggregate queries; api/routers/timeline.py histogram and additive paired list bounds; api/models/timeline.py; frontend API types/client; hooks/use-timeline-ledger.ts and use-timeline.ts; TimelinePage.tsx and bounded density component. Use existing per-butler read-model fanout and Switchboard notification read, never a general cross-schema query. WIRE: histogram requires timezone-aware minute-aligned since/until, 0<range<=24h, returns ascending 60s zero-filled buckets with source degradation; list interval optional but paired with same validation and [since,until) SQL bounds plus existing cursor. Reuse full Errors lens including failed deliveries. No content in histogram. URL: since/until chart hour, bucket_since/bucket_until one contained minute; absent chart means last complete UTC hour; Previous/Next/Latest hour; selected bucket resets list cursor and requests interval; clear/n restores live; bad URL shows recoverable invalid interval rather than broadening. Type/butler/trace retained. Counts include collapsed raw events labelled All matching events. MATRIX: >page events exact; timestamp ties lossless; boundary exclusion; empty healthy vs partial/all failed; rapid selection stale-result exclusion; browser back/reload; live arrivals do not reset historical scope; keyboard operable bar with one tab stop. ROLLBACK: deploy API additions before frontend, no old-server interval compatibility claim; reverse frontend first. DOC: approved change tasks and API contract, no unrelated spec replacement. TESTS: extend existing timeline read-model real-Postgres/API tests and TimelinePage/TimelineLedger; target +6 ~3 -0, parameterize related input dimensions. Serialize before other children owning TimelinePage. No deployment, external effects or schema migration. Existing pipeline: targeted tests first, make test-plan BASE=origin/main; real-Postgres owning API/read-model lane where SQL changes; collection for fixture topology changes; frontend lint/knip/build and make check-guards; new test files require shard registration. Broad final evidence is exact-head hosted CI/protected merge queue. Record actual Tests: +a ~b -c.  NAMED SEAMS: extend tests/api/test_timeline_summary.py for router validation/projection and add tests/integration/test_timeline_intervals.py using existing real-Postgres fixture with canonical schema stand-ins for foreign chains; extend frontend/src/pages/TimelinePage.test.tsx and components/timeline/TimelineLedger.test.tsx. No mock SQL-text assertion is accepted as aggregate correctness evidence. AVAILABILITY: new aggregates count expected source units (each selected session pool plus one selected notification unit) and healthy units; complete iff equal, partial iff 0<healthy<expected, unavailable iff expected>0 and healthy=0. Missing selected pool/table is failed, including Switchboard KeyError; do not inherit legacy list benign-skip into aggregates. Explicit unknown session target remains expected failed. Zero expected source families is No matching event sources, not fleet all-clear. Test sessions-only 2/1 and 2/0, notification-only absent pool 1/0, complete healthy zeros, and 0/0 no family. See design Explicit aggregate source availability. Bucket selection from implicit latest hour atomically persists resolved chart since/until plus bucket pair; test reload after clock/hour rollover retains that explicit scope.

### Acceptance criteria

1. >50-event fixture proves aggregate counts exceed head-page size and bucket selection fetches previously-unloaded events. 2. Exact SQL interval/type/butler/trace and Errors lens agree for stable count/list fixtures; same-time cursor and 422 cases executed. 3. Healthy zero, partial-source and all-source failure render distinguishably; aggregate DTO contains no raw content. 4. Selecting/changing/clearing interval, browser history and filter races execute destination requests and preserve correct URL/cache scope. 5. Accessible minute selection and reduced motion verified; existing unbounded list callers retain behavior. 6. Approved spec requirement reconciled, test delta and merge evidence recorded. Availability fields and per-source counts distinguish partial fan-out, all failed and absent configured pool at API and UI seams. A clock-rollover/reload regression proves chart bounds are atomically frozen with bucket selection.

## attention: Timeline recent records currently marked failed inspection strip

Type: task; priority: 3; status after creation: blocked; labels: complexity:medium, packet-complete, spec-approval-pending.

Dependencies: density; approval: exact-artifact approval; parent bu-ddo0n.

### Description

After explicit approval of the current-status option, show a last-24h inspection strip above the chronology. This replaces the original unresolved wording only with owner approval. If the owner chooses canonical unresolved conditions instead, keep this packet blocked and supersede its design through the owning specification; do not implement guessed resolution semantics.

### Design

PROPOSED, NOT ADOPTED: openspec/changes/complete-timeline-followups/. Owner approval of exact artifact revision required before dispatch. Baseline 66ed58f7f7e80963d449500141ed4cb069209967. GOVERNING REQUIREMENT: REQ-dashboard-visibility-002. SURFACES: timeline_v1 shared canonical predicates; api/routers/timeline.py GET /attention; api/models/timeline.py; frontend API/hooks and TimelinePage attention component. Depend on density child for stable filter helpers and serialize shared files. Server captures last-24h bounds once, filters by selected butler/trace, independently of chart/type, counts success=false sessions and status=failed notification attempts. Return max 5 newest {id,kind,butler,timestamp} ordered timestamp/kind/id; meta contains interval, source counts,total,has_more,degraded_sources,degraded_butlers. No prompts, summaries, recipients, errors, target URL strings or attention-ledger mutation. Strip copy: Recent records marked failed (created in last 24h), Runs, Delivery records; no unresolved/open workload claim. Link sessions by id and notifications via existing off-page /timeline?event=id, carrying butler/trace and clearing interval. Show truncation; collapse local/remount-expanded, retains count/error and ARIA disclosure. MATRIX: >5 matching rows keeps true counts; pending/success excluded; changed delivery status changes later refresh; zero only healthy, partial never all-clear; stale refresh visibly stale and retryable; no inspection side effects. No new navigation badge. DOC: record approved choice and reconcile requirement. Tests extend same real-Postgres/API seam and TimelinePage rendering/navigation, target +4 ~1 -0. Independent revert of strip/API leaves density intact. No deployment, external effects or schema migration. Existing pipeline: targeted tests first, make test-plan BASE=origin/main; real-Postgres owning API/read-model lane where SQL changes; collection for fixture topology changes; frontend lint/knip/build and make check-guards; new test files require shard registration. Broad final evidence is exact-head hosted CI/protected merge queue. Record actual Tests: +a ~b -c.  NAMED SEAMS: extend tests/api/test_timeline_summary.py and the density child real-Postgres tests/integration/test_timeline_intervals.py for attention count predicates; extend frontend/src/pages/TimelinePage.test.tsx. Foreign-schema fixture DDL comes from src/butlers/testing/schema_standins.py. AVAILABILITY: new aggregates count expected source units (each selected session pool plus one selected notification unit) and healthy units; complete iff equal, partial iff 0<healthy<expected, unavailable iff expected>0 and healthy=0. Missing selected pool/table is failed, including Switchboard KeyError; do not inherit legacy list benign-skip into aggregates. Explicit unknown session target remains expected failed. Zero expected source families is No matching event sources, not fleet all-clear. Test sessions-only 2/1 and 2/0, notification-only absent pool 1/0, complete healthy zeros, and 0/0 no family. See design Explicit aggregate source availability. MUTABLE STATUS: notification ack-failed and retry claiming both write failed->read before recovery. Count only records created in window whose current status is failed; acknowledgment/retry claim remove them on refresh without proving recovery. Healthy zero copy No matching records currently marked failed. Never claim no failures occurred or successful delivery recovery. Add explicit acknowledgment and retry-claim-before-success API/DOM cases.

### Acceptance criteria

1. Exact server counts and five-row cap are independently proven over >5 failures; IDs/order and last-24h cutoff correct. 2. Pending/success excluded; records currently marked failed honestly labelled rather than unresolved or immutable failed occurrences, and changing status reflected on refresh. 3. Links execute exact session/event destinations preserving applicable scope without mutating state. 4. Collapse, truncation and healthy-empty/partial/all-source failure execute their user-visible behavior; new DTO excludes content fields. 5. Spec choice approval, targeted test delta and merged evidence recorded. Availability fields and per-source counts distinguish partial fan-out, all failed and absent configured pool at API and UI seams. Notification acknowledgement and retry-claim transitions exclude rows before success, without any historical-no-failure or recovery claim.

## keyboard: Timeline real-focus row traversal and existing-view palette actions

Type: task; priority: 3; status after creation: blocked; labels: complexity:medium, packet-complete, spec-approval-pending.

Dependencies: attention; approval: exact-artifact approval; parent bu-ddo0n.

### Description

Complete keyboard navigation through current rendered timeline disclosures and expose existing view presets in the existing action system. Preserve global search ownership; no new local search, pagination shortcut or alternate keybinding service.

### Design

PROPOSED, NOT ADOPTED: openspec/changes/complete-timeline-followups/. Owner approval of exact artifact revision required before dispatch. Baseline 66ed58f7f7e80963d449500141ed4cb069209967. GOVERNING REQUIREMENT: REQ-dashboard-visibility-003. SURFACES: TimelinePage.tsx and components/timeline/TimelineLedger.tsx; reuse hooks/use-page-actions.ts and existing shortcut/context registry, drawer choreography. Serialize after attention. j/k focuses rendered primary disclosures in visual order; first j first row, first k last row, clamp at ends; collapsed heartbeat/maintenance group is one row. Enter native activation only, never global second handler; no implicit page fetch. Preserve focus identity through refresh or nearest surviving index on deletion. Suppress typing/contentEditable/IME/modal/modifier contexts via registry. r and conditional n unchanged except approved density clear behavior; / and Cmd/Ctrl+K remain global palette search; Escape uses shell focus restore. Add All/Errors only/Notifications palette-only useRegisterCommands commands invoking existing selectBuiltInView without duplicate state logic. Keyboard help consumes actual shortcut registrations; palette-only commands carry no invented key. Use useListTriage/useRegisterShortcut with real-focus adapter rather than forcing presets into key-required usePageActions. MATRIX: focus identity under updates, group rows, empty feed no-op, bounds clamp, nested drawer ownership, inputs ignored, one activation. No persistence/wire changes. DOC: approved spec scenarios and user-facing help/copy inventory if modified. Extend TimelinePage.a11y.test.tsx, TimelineLedger tests and existing shell tests as reuse evidence; target +3 ~2 -0; no source-scan duplicate tests. No deployment, external effects or schema migration. Existing pipeline: targeted tests first, make test-plan BASE=origin/main; real-Postgres owning API/read-model lane where SQL changes; collection for fixture topology changes; frontend lint/knip/build and make check-guards; new test files require shard registration. Broad final evidence is exact-head hosted CI/protected merge queue. Record actual Tests: +a ~b -c. 

### Acceptance criteria

1. Browser DOM tests prove j/k real focus, first/last entry, clamping, empty behavior, group traversal and no hidden pagination. 2. Enter opens once and Escape returns focus; drawer and typing/IME shortcuts do not leak. 3. Refetch retains identity or nearest survivor without stealing focus from another control. 4. Existing r/n/global search and preset URL/request behavior remain intact; presets are palette commands and real shortcut keys appear in help. 5. Spec scenario reconciliation and focused test/actual delta evidence recorded.

## reconciliation: Reconcile the approved One Timeline follow-ups across the combined flow

Type: task; priority: 3; status after creation: blocked; labels: complexity:medium, packet-complete, spec-approval-pending.

Dependencies: density, attention, keyboard; approval: exact-artifact approval; parent bu-ddo0n.

### Description

Perform one cross-child closeout after density, attention and keyboard are merged. Reconcile actual delivery and approved scope; do not claim the parent complete merely because child tests passed.

### Design

PROPOSED, NOT ADOPTED: openspec/changes/complete-timeline-followups/. Owner approval of exact artifact revision required before dispatch. Baseline 66ed58f7f7e80963d449500141ed4cb069209967. GOVERNING REQUIREMENT: REQ-dashboard-visibility-001/002/003. OWNERSHIP: combined browser journey, exact merged evidence, approved delta archive and parent report. No standalone redesign or duplicate implementation. Verify API-before-client interval rollout, notification event navigation clearing interval, bucket navigation to unloaded history, keyboard drawer open/close and browser back, all while degraded response is honest. Reuse children named invariants/test receipts; add at most one cross-feature browser flow only if existing tests do not already exercise the seam. Target +1 ~0 -0 maximum, justify actual delta. Check merged commits and hosted CI exact heads, inspect same-named active requirement bodies before archive. Archive only completed approved scenarios; if attention option was not approved, parent cannot claim full original completion: retain explicit owner gate and reconcile only approved subset. No destructive data or deployment action. DOC: archive tasks/spec and parent closure evidence; update applicable status references if needed. Failure returns a concrete correction to owning child rather than masking gap. No deployment, external effects or schema migration. Existing pipeline: targeted tests first, make test-plan BASE=origin/main; real-Postgres owning API/read-model lane where SQL changes; collection for fixture topology changes; frontend lint/knip/build and make check-guards; new test files require shard registration. Broad final evidence is exact-head hosted CI/protected merge queue. Record actual Tests: +a ~b -c. 

### Acceptance criteria

1. Every approved mandatory scenario maps to merged implementation and named behavior evidence. 2. Combined notification link -> interval select -> keyboard drawer -> back navigation succeeds, including independently degraded source UI. 3. Active delta collision scan and strict validation pass without unrelated clause losses; archive matches approved delivered scope. 4. Exact merged/CI evidence recorded; parent closes only after all approved required work and archive, with unresolved original intent not silently erased.
