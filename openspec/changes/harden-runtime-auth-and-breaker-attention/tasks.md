## 1. Durable state and authorization foundations

- [x] 1.1 Add a guarded core migration for `public.runtime_attention_outbox`, immutable source snapshots, triggering-edge and direct-reissue unique constraints, fenced-claim fields/indexes, non-cascading retention semantics, Switchboard claim grants, and fixed-search-path `SECURITY DEFINER` producer operations with `PUBLIC` execution revoked before role-specific grants; derive all pageable source/payload data server-side without claiming the shared-login `SET ROLE` topology is an unforgeable per-butler identity; write migration tests for upgraded, core-only, catalog-deletion, and role-enforced databases.
- [x] 1.1a Stage nullable, bounded terminal delivery-error class/detail and optional scalar notification-reference fields in the inert core outbox; preserve core-only installation, no cross-schema foreign key, no runtime evidence-update grant, and no producer/worker activation; add real-Postgres migration coverage.
- [x] 1.2 Add a single serialized qualifying-dispatch outcome recorder used by Spawner failure/success provenance, including stable attempt-ID ordering, per-catalog-entry transaction serialization, same-transaction breaker-edge append through the authorized producer operation, and safe degraded-provenance behavior when that transaction cannot commit; replace direct model-breaker alert invocation.
- [x] 1.3 Move the fleet-halt breach-edge producer onto the same recorder/outbox facility and remove its direct audit-marker/ledger/notification debounce path without backfilling historic breaches.

## 2. Canonical Codex CLI-auth authority

- [x] 2.1 Extend `CredentialStore` with an explicit system-global authority channel and strict `cli-auth/codex` read/write operations that cannot silently fall back to a schema-local value; preserve existing local-first behavior for ordinary domain credentials and existing other-provider CLI-auth behavior.
- [x] 2.2 Refactor Codex CLI-auth persistence, lifecycle restoration, daemon construction, live Codex reconciliation, dashboard device-auth/probe paths, and Codex-dependent connector startup to pass and use the explicit authority; retain only safe local-conflict diagnostics.
- [x] 2.3 Add focused credential and runtime tests covering stale local versus fresh authority, flat topology, multiple daemons sharing one Codex home, unavailable authority, conditional rotation/health fencing, atomic file writes, and no credential-content logging.

## 3. Canonical OpenCode identity, execution mapping, and verification

- [x] 3.1 Correct the named pure OpenCode canonical-to-execution mapper: preserve `opencode-go/<id>` in catalog/pricing/spend/provenance and pass that same provider-qualified value to the current OpenCode CLI, which requires `provider/model`. Apply it to normal adapter command construction, the pinned OpenCode CLI-auth health check, and any generated configuration field that represents the selected OpenCode Go execution model. The current OpenCode Go JSONC has no such field and must not invent one; preserve provider-registration-specific configuration transforms outside this mapper's scope, and leave catalog defaults and historical qualified identifiers unchanged.
- [x] 3.2 Correct parameterized adapter/health-command tests for `opencode-go/minimax-m2.7`, `opencode-go/mimo-v2.5`, and `opencode-go/minimax-m3`, plus qualified-provider, unrelated-runtime, and existing-bare no-rewrite controls; prove canonical pricing/spend/history lookup remains intact.
- [x] 3.3 Implement the deterministic Switchboard-owned runtime-probe coordinator behind a private authenticated dashboard control-plane command, not the generic MCP/LLM tool surface. Dashboard-triggered Test/Verify first require fail-closed dashboard-owner control; a dedicated Dashboard/Scheduler-to-Switchboard `runtime_probe_control` client signs a one-minute Ed25519 JWS capability when its deployment key is available. Require protected `alg=EdDSA`/key ID resolved only from the deployment keyring (no dynamic key lookup; reject `jku`/`x5u`), fixed `switchboard.runtime_probe_control.v1` audience, 256-bit nonce, and time claims `iat <= now + 5s`, `exp >= now - 5s`, and `0 < exp - iat <= 60s`; reject `none`, symmetric, other asymmetric, or token-selected algorithms. Add the Switchboard-only `runtime_probe_control_receipts` migration with unique audience/nonce digest, commit the receipt before lookup/launch/persistence, store neither raw nonce nor signature, and retain it through at least `exp + 5s`. Switchboard receives only a non-secret verification key. The coordinator accepts only a catalog ID, uses the resolved catalog entry, shared runtime home, applicable authority, adapter construction, mapping, generated config, and runtime args, with bounded timeout, per-entry de-duplication, and global concurrency without domain MCP tools or routed provenance. Land endpoint/client code dark and test it with isolated fixture key files; do not add production signer or verifier mounts in this propagation phase, so the deployed path remains unavailable.
- [ ] 3.4 After task 3.6b proves every remaining Dashboard runtime CLI child has an exclusive kernel-enforced invocation domain, peer isolation, and complete descendant termination before staged-output validation, activate the Dashboard signer and shared verifier mounts through the canonical full-stack launcher. Dashboard may become healthy before all-butlers, but its signed client must remain unavailable and sign nothing until Switchboard's private non-secret readiness response confirms the matching verifier key ID. In the same Dashboard image, route per-model Test, verify-all, and scheduled verification through the coordinator and remove every dashboard-local direct adapter probe path. Preserve verification history semantics and return honest unavailable/rate-limited states. Test default and hotreload full-stack startup, readiness mismatch, and rollback retaining the child sandbox or removing the signer before an older image starts. Test missing signing material/no-config `503` and missing/wrong-owner-key `401` before any Switchboard work; test invalid Ed25519 signature, `none`/symmetric/unsupported/dynamic-key algorithm path, wrong audience/catalog/caller, unknown key ID, future `iat`, expired `exp`, and invalid/overlong lifetime before receipt creation/lookup/launch/persistence; race two equal valid capabilities in real Postgres and prove one receipt/one lookup-launch-persist path; prove receipt cleanup retains it through `exp + 5s` and non-Switchboard direct receipt-table access is rejected; prove replay denial after restart; prove generic-MCP denial; prove the scheduler is the only registered internal caller; prove private-key and capability/signature redaction/no fallback; prove generic Secrets inventory/detail/mutation/audit cannot expose or create the reserved signing-key/fingerprint; prove `accept_until - sign_until` is within `[70 seconds, 5 minutes]`, every pre-cutover capability remains valid through its at-most-60-second lifetime plus allowed request skew, retiring-key `iat > sign_until` is rejected without a cutover-skew exception, and the retiring key is rejected after `accept_until`; and prove probe success never closes a breaker.
- [x] 3.5 Implement the fixed asymmetric deployment-key parsers, validation, and documentation under the governing process-bound deployment-control-key classification in `about/heart-and-soul/security.md`: accept the strict private signer snapshot only from `/run/secrets/runtime_probe_control_signing_key` in Dashboard API/scheduler; accept the same strict public keyring snapshot from `/run/secrets/runtime_probe_control_verifiers` in Dashboard and all-butlers so Dashboard can verify its configured current or retiring signer while all-butlers receives no private material; validate at startup, reload only on owning-process restart, and fail only probe control closed. Implement and document readiness-gated canonical full-stack rotation with the retiring-key overlap. Representation and propagation changes must not add either production mount; mount activation belongs to task 3.4 after the task 3.6b sandbox. Verify no all-butlers daemon (including Switchboard), runtime child, browser-facing Secrets API, environment-value fallback, or generic environment dump can read the private signing key.
- [x] 3.6a Propagate the explicit system-global Codex authority through the existing model-settings Test/verify and hourly verification paths without changing their direct-adapter deployment shape; preserve non-Codex construction and skip Codex verification without a `last_verified_ok=false` write when explicit authority is unavailable.
- [x] 3.6b Before any production signer mount, inventory every direct and aliased Dashboard runtime-CLI spawn path across provider listing/health, device auth, API-key testing, Codex prewarm, Settings Console, Secrets reauthorization/probe-all, and model verification. The existing model-settings Test/verify adapter paths remain deliberately deferred **only** for the coordinator/sandbox cutover in task 3.4; this authority-propagation phase MUST still pass the explicit system-global Codex authority through their current direct adapter construction, preserve non-Codex construction, and skip Codex verification without a `last_verified_ok=false` write when that authority is unavailable. Do not add a coordinator or Dashboard sandbox migration in this sub-slice; task 3.4 removes these local adapter paths atomically at cutover. Remove Dashboard Codex prewarm and any other non-verification adapter execution. Install an exact Bubblewrap package version in the base image and record it in the image manifest and route every remaining CLI-auth child through one launcher that allocates an exclusive non-reused outer UID/GID from `61000..61999`, clears supplementary groups, sets `no_new_privs`, and enters nested user/mount/PID/IPC/UTS namespaces with fresh procfs, private tmpfs, minimal devices, one writable staged HOME, exact read-only executable/library/CA/resolver inputs, and no root/run/app/canonical-home/peer-stage/host-proc view. Spawn Bubblewrap with `close_fds=True` and an exact typed `pass_fds` allowlist limited to safe stdio and launcher-created setup/seccomp descriptors whose referents are not credentials or protected paths; run a repository-owned namespace-PID1 shim that uses `close_range(3, UINT_MAX, 0)`, verifies no descriptor above stderr remains, and then execs the provider CLI. Use the same repository-owned namespace-capable seccomp policy, including `close_range`, plus `apparmor:unconfined` and `systempaths=unconfined` for default and hotreload Dashboard without `privileged`, `cap_add`, host PID, or Docker socket. Fail CLI-auth launch and signer activation closed when the exact-image preflight, pidfd support, close-range/shim checks, descriptor-referent checks, or UID pool is unavailable; never fall back to a direct subprocess. For health/API-key tests, have the trusted parent stage a validated regular-file authority copy and discard all child changes without writeback. For device auth, use `--as-pid-1 --die-with-parent` and a trusted `--info-fd`/`--block-fd` handshake, open a pidfd for the reported namespace PID 1 before releasing the payload, and after every direct-child outcome signal and boundedly escalate that pidfd until death before separately waiting for the direct Bubblewrap child; never `waitid` the non-child PID 1. Only then open output relative to a trusted root descriptor with `openat2` beneath/no-symlink constraints, validating and consuming it through that same descriptor, then compare-and-set persist through explicit authority and clean the staging tree before releasing the identity. Add adversarial exact-Compose tests proving concurrent child A cannot read/write child B's stage or inspect peer process state; an intentionally inheritable secret/canonical/staging descriptor is absent and no descriptor above stderr reaches provider code; fork/double-fork/`setsid` descendants cannot survive, mutate output after direct-child exit, or cause persistence; and every inherited-FD/containment/path/link/alternate-credential/write-outside-provider-scratch/owner-mode-size-schema/authority-race/cancel/timeout failure persists nothing. Define exact provider-specific disposable scratch roots for CLI database/WAL/log/config/package files, persist only the expected same-FD credential artifact, and discard all scratch content. Also prove the child cannot open the root-owned mode-0400 signer, protected parent environment, or canonical credential path, and add a completeness scan that fails on any non-verification runtime CLI subprocess bypass. Keep the sandbox in every rollback while the signer mount exists.
- [ ] 3.6c After the proposed Option B amendment receives required security/engineering review and adoption, harden the existing launcher so the image build emits schema-version-3 `shim` inputs only after the final PID1 shim install and every `build_bubblewrap_launch_plan` caller supplies that validated closure explicitly. Update `scripts/generate_runtime_cli_sandbox_manifest.py`, `Dockerfile.base`, `src/butlers/cli_auth/sandbox_platform.py`, the production launcher, all four exact-image harness callers, and the existing generator/sandbox/Dockerfile tests as one matched image/application change. Deduplicate identical mappings deterministically, reject conflicts and malformed/missing/old-version/image-mismatched closure before spawn, keep the planner free of manifest I/O and runtime `ldd`, retain the single spawn-boundary policy unchanged, and do not use or delete the prior Option A prototype. Implementation remains owned by `bu-9hkm5`; this unchecked task is a cold-start handoff, not authorization or reassignment.

## 4. Switchboard-owned operational attention delivery

- [x] 4.1 Implement the outbox repository and Switchboard worker with a single delivery-service lease, fenced `pending` claim, `sending` pre-transport commit, pre-transport token check, bounded external transport deadline, bounded proven-pre-send retry, conditional `sent`/`failed`/`uncertain` transitions, dead-claim recovery that never replays, and safe structured observability.
- [x] 4.2 Refactor Switchboard notification routing so a confirmed Messenger send remains confirmed when routing-log, registry, notification-log, audit, or attention-ledger bookkeeping fails; preserve clear not-attempted versus uncertain outcomes.
- [x] 4.3 Add real-Postgres role-isolation and concurrency integration tests proving the authorized producer operation rejects forged source/payload data, `PUBLIC`/`connector_writer`/non-producer effective roles cannot execute it, producers have no raw outbox access, and Switchboard alone claims/delivers; document that these are effective-role checks rather than independently authenticated per-butler principal proof.
- [x] 4.4 Add real-Postgres lifecycle tests that seed an open/cooldown-expired breaker, race distinct failed half-open attempts, and assert one new episode and one delivery claim; race two workers and assert one transport invocation; persist `sending`, restart, assert zero replay transport calls and terminal `uncertain`; and simulate slow live claimant versus recovery versus reissue to prove fencing and one successor.
- [x] 4.5 Add route/outbox tests proving a confirmed Messenger result survives post-send ACL bookkeeping failure, an ambiguous send is never automatically replayed, and recorder/outbox persistence failure causes no partial episode or direct delivery.

## 5. Operator-facing truth and deliberate recovery

- [x] 5.1 Extend the Models API with fail-closed dashboard-owner-control safe latest-attention-episode observation, explicit runtime-probe semantics, and an atomic state-checked idempotent endpoint that creates or returns at most one successor for a confirmed uncertain episode; add no-config `503`, missing/wrong-key `401`, valid-key success, no-observation, no-successor, and no-transport regression coverage.
- [x] 5.2 Update `SettingsModelsPage` and its tests to render verification, routing eligibility, breaker state, and attention state independently; add an accessible confirmation-gated `Send a new alert` action only for eligible uncertain episodes, while treating the fail-closed server-side dashboard-owner-control dependency as the security boundary.
- [x] 5.3 Keep Spend fleet-halt visibility intact while reflecting the new durable attention evidence where available; ensure degraded outbox/attempt sources are shown as unavailable rather than no-alert/no-denial states.
- [x] 5.4 Add safe logs, counters, and operator diagnostics for authority selection, ignored local CLI-auth scope, breaker-edge episodes, delivery outcomes, and coordinator availability without secret or raw provider-error disclosure.

## 6. Contract, regression, and runtime verification

- [x] 6.1 Add requirement-ID citations to the focused unit, API, migration, ACL, concurrency, adapter, and frontend tests introduced by this change; run the relevant tests first, then the affected backend/frontend suites.
- [x] 6.2 Reconcile the changed capability specifications and implementation documentation, including canonical qualified OpenCode defaults/pricing identity, execution-boundary mapping, private probe control semantics, and the Models-page distinction between a runtime probe and breaker recovery. Keep them aligned with the governing process-bound deployment-control-key classification in `about/heart-and-soul/security.md`.
- [ ] 6.3 After code review and deployment authorization, validate the exact deployed runtime: authority provenance without revealing values, one shared Codex auth restore result, canonical provider-qualified OpenCode execution resolution, one alert per breaker edge, fenced/no-replay ambiguous delivery, `/api/health`, and the Models/Spend API/UI truth surfaces.

## Reconciliation record

`reconciliation.md` (bead `bu-0uqgo.7`) records the generation-1 falsification against head
`02277aa88`: the requirement-to-evidence matrix, the test-instrument audit, the gate results,
and the gap register. Task 3.4 stays open on one missing clause (owner control on Test and
Verify-all) and 6.3 stays open pending separate owner authorization for deployed-runtime
evidence.
