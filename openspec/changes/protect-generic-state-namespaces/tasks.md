## 0. Review and owner gate

- [ ] 0.1 Independently review this exact proposed change against current
  central owner auth, core-state, dashboard State CRUD, the active adopted Home
  presence change, MCP session sandboxing, and content-blind security doctrine.
- [ ] 0.2 Obtain the owner's explicit Option A adoption or Option B accepted-risk
  choice. Silence preserves current behavior and the open P1; it is not risk
  acceptance or implementation authority.
- [ ] 0.3 Recheck active OpenSpec requirement ownership and the current Home
  implementation bead before dispatch. Do not create a competing MODIFIED block
  or split Home protection from its replacement route.

## 1. Future policy and exposed-boundary implementation

- [ ] 1.1 Implement the immutable per-butler exact/prefix/suffix policy registry,
  conflict detection, initial matrix, and new-namespace classification guard.
- [ ] 1.2 Enforce dashboard list/get/set/delete decisions before protected value,
  pool, MCP-client, or state access while preserving ordinary-key compatibility.
- [ ] 1.3 Enforce MCP list/get/set/delete decisions before low-level state calls,
  including direct invocation through the authenticated dashboard tool-call tab.
- [ ] 1.4 Keep deterministic internal and specialized callers on low-level state
  seams; do not add a migration, rewrite values, or make mutable state an
  authority source.

## 2. Atomic Home implementation

- [ ] 2.1 Under one implementation owner and PR, implement the adopted dedicated
  Home presence GET/PUT contract and activate its exact managed-key policy entry.
- [ ] 2.2 Preserve the adopted validation, local reference, advisory-lock, CAS,
  transaction, audit, malformed-state, producer, and privacy behavior.
- [ ] 2.3 Reconcile the active Home change's honest-boundary text only after the
  implementation is real; retain unrelated clauses and do not overclaim live or
  deployed exclusivity.
- [ ] 2.4 Keep Chronicler SSID generic dashboard CRUD available until a reviewed
  replacement owner editor lands; deny its MCP operations without reading a
  live mapping.

## 3. Verification and handoff

- [ ] 3.1 Run pure policy and namespace-inventory tests for exact/prefix/suffix,
  conflicts, every matrix operation, and ordinary compatibility.
- [ ] 3.2 Run mounted owner-auth/API and MCP tests proving pre-access denials,
  keys-before-values listing, direct tool-call resistance, and content-blind
  errors/audit/logs/telemetry.
- [ ] 3.3 Run focused migrated-PostgreSQL Home route/CAS/race/rollback/producer
  tests and specialized module/settings/threshold compatibility tests.
- [ ] 3.4 Report `Tests: +a ~b -c`; run test-plan, collection when topology moves,
  Ruff, strict OpenSpec, overwrite/countable/guards/budget, terminal hosted CI,
  and fresh independent exact-head review.
- [ ] 3.5 Treat merge, deployment, restart, live state access/mutation, private
  identifier submission, and broader namespace classification as separate
  authority. Archive only after adopted requirements and implementation land.
