## 0. Independent review and owner gates

- [ ] 0.1 Obtain independent exact-head review of this proposed change against the current Dispatch baseline, active deltas, the landed #3716 visual-role and #4194 focus contracts, and the original P1 `bu-6jv4m.12`.
- [ ] 0.2 Record the owner's explicit choice: A, adopt the route-state, debt, keyboard, and composited-contrast contract; or B, hold this proposal and explicitly retain the narrower current coverage/risk without claiming AA completion. Silence chooses neither.
- [ ] 0.3 If A is adopted, obtain a separate explicit release or reassignment of tze-owned `bu-6jv4m.12` before dispatching implementation leaves. Adoption alone does not change its assignee or authorize live/private-data work.

## 1. Route/state inventory leaf

- [ ] 1.1 Derive exact static, parameterized, and redirect scenario identities from the shell capability manifest and router; make a route or consequential state with no evidence or bounded debt fail the completeness gate (REQ-dashboard-design-language-003).
- [ ] 1.2 Convert the two current static skip entries and missing dynamic/state cases into exact, owner-named, replacement-test debt records with a review date within 30 days; fail expired, wildcard, duplicate, and silent gaps without labelling them passes (REQ-dashboard-design-language-005).
- [ ] 1.3 Build synthetic non-private data-hook fixtures for real page components covering loaded and every applicable degraded/empty state; keep loading-only evidence separate (REQ-dashboard-design-language-003/005).

## 2. Route/keyboard behavior leaf

- [ ] 2.1 Extend production-component axe and reusable keyboard tests for primary controls and consequential overlays, including focus entry, operation, allowed Escape dismissal, focus return, and semantic result announcement (REQ-dashboard-design-language-004).
- [ ] 2.2 Cover QA's Butler menu, Memory Search, Ingestion filters/timeline, and dynamic Entity/Bead/QA dossiers at their real component seams; correct only confirmed inaccessible behavior while preserving existing route and data contracts (REQ-dashboard-design-language-004).
- [ ] 2.3 Confirm static/dynamic/redirect inventory parity, no private fixture values, no mock replacement pages, and no false coverage claim from pending-fetch shells (REQ-dashboard-design-language-003/005).

## 3. Composited contrast leaf

- [ ] 3.1 Extend existing contrast math and a deterministic both-theme actual-style fixture to measure alpha-composited semantic foregrounds against supported page, elevated, nested, and attention-tinted surfaces; include known failing `--mfg` 70% small-text cases (REQ-dashboard-design-language-006).
- [ ] 3.2 Add a source guard for informational or operable semantic text opacity; allow only an exact measured qualifying large-text use, while preserving the existing disabled-control opacity and background attention-tint rules (REQ-dashboard-design-language-006).
- [ ] 3.3 Migrate current small-text alpha violations, beginning with Ingestion labels, to opaque AA-safe roles; verify typed role parity and the landed global focus boundary without inventing a second focus system (REQ-dashboard-design-language-006).

## 4. Verification and handoff

- [ ] 4.1 For each separately owned implementation PR, state `Tests: +a ~b -c`, run focused red-to-green cases and the dirty-worktree test planner, then frontend lint, copy guards, Knip, build, tests, strict OpenSpec, overwrite/countable/guards, and exact-head hosted CI.
- [ ] 4.2 Obtain independent accessibility and spec review of the combined evidence, including keyboard flow, screen-reader announcements, both-theme computed ratios, route-state debt expiry, and compatibility with active Dispatch deltas before any claim that `bu-6jv4m.12` is complete.
- [ ] 4.3 Keep browser exercise synthetic and non-private. Treat any production/private-data inspection, deployment, owner-policy exception, or external effect as a separate authorization gate.
